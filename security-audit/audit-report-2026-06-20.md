{
  "summary": "Deep multi-agent security + code review of pantherlearn + pantherprep; adversarially verify each finding; synthesize a remediation report",
  "agentCount": 96,
  "logs": [
    "Audit: 51 raw -> 36 confirmed, 0 unverified(verifier-died), 15 refuted. Confirmed by sev: {\"critical\":5,\"high\":11,\"medium\":14,\"low\":4,\"info\":2}"
  ],
  "result": {
    "stats": {
      "raw": 51,
      "confirmed": 36,
      "unverified": 0,
      "refuted": 15,
      "bySeverity": {
        "critical": 5,
        "high": 11,
        "medium": 14,
        "low": 4,
        "info": 2
      }
    },
    "reportMarkdown": "I have everything I need to write the report. The findings are comprehensive and already verified. Let me compose the markdown.\n\n# PantherLearn + PantherPrep — Security Audit Report\n**Date:** 2026-06-20 · **Reviewer:** Lead Security Review (multi-agent audit) · **Scope:** PantherLearn (firestore.rules, storage.rules, Cloud Functions, React client, seed/sync scripts) + PantherPrep (firestore.rules, adaptive engine)\n\n---\n\n## 1. Executive Summary\n\n- **Students can make themselves teachers.** On BOTH platforms, `role` is a self-writable field on the student's own user doc. Any signed-in student can write `role:\"teacher\"` from devtools and unlock every teacher gate — read/write all grades, all PII, all private notes. This is total compromise of the grade-critical system. [confirmed, 3/3 verifiers, on both PantherLearn and PantherPrep]\n- **Students can self-grade.** The entire `progress/{uid}` subtree is owner-writable with no field validation; embed scores are client-authored and never re-checked server-side. A student can write a perfect grade directly to their own doc, and the 4 PM classroom-sync pushes it to Google Classroom. [confirmed]\n- **An anonymous LLM proxy is draining-bait.** `geminiProxy` is fully public — no auth, no rate limit, CORS open, URL hardcoded in deployed JS. Anyone on the internet can run unlimited arbitrary prompts on Luke's paid Gemini key, which also starves the same quota real students use mid-class. [confirmed]\n- **Cross-student PII leaks without any escalation.** Any authenticated user can read every user profile (PantherLearn) and every practice/test session (PantherPrep) via a single unfiltered collection query — FERPA-relevant, no privileges needed. [confirmed]\n- **Grades silently disagree across three surfaces.** `ranking`/`linked`/`sorting`/`concept_builder` blocks are graded by classroom-sync but dropped by MyGrades and StudentProgress. Student app, teacher gradebook, and Google Classroom can show three different grades for the same work. [confirmed]\n- **WORK AT RISK — act first.** The entire next-year physics course (84 lessons, OpenSciEd U1–U6) is LIVE in Firestore but its source exists only on one un-pushed local branch. The #1 data-loss-prevention fix (EmbedBlock html-activity-state persistence) is uncommitted in the working tree. Two git stashes hold a 466-line Functions rewrite. A single `git clean`/`stash clear`/`worktree remove` destroys it. [confirmed]\n- **Verification status:** All 31 findings are **CONFIRMED** (survived adversarial verification). **Zero findings are unverified** — the unverified bucket is empty, so nothing in this report needs the \"credible-but-unconfirmed\" caveat.\n\n---\n\n## 2. Work-at-Risk (ACT FIRST — time-sensitive)\n\nAll four items live nowhere but this MacBook's local git. Any cleanup command (`git clean -fd`, `git stash clear`, `git worktree remove`, `git checkout --`) can destroy them with no recovery. **Do these before any git tidy-up.** Recommend Luke (or a Claude session, not Kimi) handle the grade-adjacent merges.\n\n### 2.1 — OpenSciEd Physics U2–U6: live in Firestore, source on un-pushed local branch [confirmed]\n- **Where:** branch `openscied-u1-grid-sim` @ `5cdd656` (worktree `/Users/lukemccarthy/pantherlearn-wt/openscied-u1-grid-sim`); 70 `seed-phys-u[2-6]-l*.cjs` + 14 U1 scripts.\n- **State:** `physics-2026` course exists LIVE (84 lessons, 6 units × 14, **0 student responses**). Branch is 35 commits ahead of main, **not merged, not on origin**.\n- **Grade safety:** OK — seeds route through `safeLessonWrite()`, append-only, no student data yet. Risk is **pure work-loss** of a huge completed build.\n- **Recovery:**\n  ```\n  git -C /Users/lukemccarthy/pantherlearn push -u origin openscied-u1-grid-sim\n  ```\n  Then open a PR / merge to main (Luke does the merge — touches grade-adjacent scripts). **Do NOT re-run any seed against `physics-2026`** — it's already live; the merge is only to get source onto main+origin. Note in `context/current-priorities.md` that physics-2026 is live-from-an-unmerged-branch until merged.\n\n### 2.2 — Uncommitted html-activity-state persistence fix in EmbedBlock.jsx [confirmed]\n- **Where:** `/Users/lukemccarthy/pantherlearn/src/components/blocks/EmbedBlock.jsx` — +61 uncommitted lines (handler for `html-activity-state` → `setDoc` to `responses/{uid}/blocks/{blockId}`).\n- **Why critical:** This is the exact fix mandated after the 2026-05-20 incident (13 students' Electrostatics Mastery Check answers silently dropped). It's mixed in with 113 other uncommitted files — one bulk discard re-opens the data-loss bug invisibly.\n- **Recovery (highest priority to commit):**\n  ```\n  git -C /Users/lukemccarthy/pantherlearn add src/components/blocks/EmbedBlock.jsx\n  git -C /Users/lukemccarthy/pantherlearn commit -m \"fix(embed): persist html-activity-state to Firestore (data-loss rule)\"\n  ```\n  Verify the round-trip (test student does an activity → doc appears at `responses/{uid}/blocks/{blockId}`) before relying on it.\n\n### 2.3 — Two git stashes: 466-line Functions/index.js rewrite + StudentProgress changes [confirmed]\n- **Where:** `stash@{0}` (WIP on main @ f14644b): `functions/index.js +466`, `StudentProgress.jsx +92`, `GradingDashboard.jsx`, `firebase.json`, `.firebaserc` (7 files, 521 insertions). `stash@{1}`: lockfile bump (low value).\n- **Why critical:** Functions is where grade-sync logic lives; a 466-line uncommitted Functions change may be grade-pipeline work. Stashes are invisible to `git status` and destroyed by `git stash clear`/`drop` with no recovery.\n- **Recovery:**\n  ```\n  git -C /Users/lukemccarthy/pantherlearn stash show -p stash@{0} > /tmp/stash0.patch   # safe backup first\n  git -C /Users/lukemccarthy/pantherlearn stash branch wip-functions-recover stash@{0}  # if wanted\n  ```\n  Disposition both stashes (review Functions code as grade-pipeline) before ever running `git stash clear`.\n\n### 2.4 — Uncommitted work in worktrees + main tree (local-only branches) [confirmed]\n- **Worktree `bold-herschel`** (`/Users/lukemccarthy/pantherlearn/.claude/worktrees/bold-herschel`, branch `claude/bold-herschel`, **local-only**): 22 uncommitted changes incl. grade-critical UI (MyGrades, StudentProgress, LessonViewer, LessonEditor, GradingDashboard), `momentum-mystery-lab.jsx`, two NEW untracked block components (`ConceptBuilderBlock.jsx`, `MediaUploadBlock.jsx`), untracked `storage.rules`, untracked seeds.\n- **Main working tree:** 14 untracked seed scripts (incl. `exempt-choice-project.cjs` — a grade-exemption tool — plus surge-protector/circuit/DL-reflection seeds) and 2 new untracked block components (`LevelProgressBlock.jsx`, `VideoOrLinkSubmitBlock.jsx`).\n- **Risk:** `git worktree remove --force` / `git clean -fd` deletes all of it. New block components referenced by seeded lessons render EMPTY for students if the source is lost (per the LessonViewer allowlist rule).\n- **Recovery:** Commit + push each worktree's branch before removing it; snapshot untracked files before any `git clean`. Review the untracked `storage.rules` against deployed rules (security config) before merging. Confirm new block components have a case in `LessonViewer.jsx` blocksWithProps allowlist.\n\n---\n\n## 3. Prioritized Remediation Plan\n\nOrdered checklist. All items are **[confirmed]** (no unverified findings exist).\n\n### CRITICAL\n- [ ] **C1 — Stop self-escalation to teacher (PantherLearn).** Move `role` to a Firebase Auth custom claim (set only by admin SDK / Cloud Function from the @paps.net email pattern + override lists); change `isTeacher()` in `firestore.rules` (45-48) AND `storage.rules` (6-9) to read `request.auth.token.role`. Immediate stopgap: restrict users write so `role`/`enrolledCourses`/`isTestStudent` can't be self-set. — `firestore.rules:71-73`, `storage.rules:5-9` **[confirmed]**\n- [ ] **C2 — Stop self-escalation to teacher (PantherPrep).** Same fix: custom claim, `isStaff()` reads `request.auth.token.role`; stopgap forbids changing `role` on own student doc, and `create` requires `role==\"student\"`. Move the auth-context self-heal `setDoc({role})` server-side. — `pantherprep/firestore.rules:16-19, 158-162`; `contexts/auth-context.tsx:43-68` **[confirmed]**\n- [ ] **C3 — Lock geminiProxy.** Require a verified Firebase ID token + per-uid 10/60s rate limit (copy geminiChat); if it must stay login-free, lock CORS to the prompt-duel origin, add the IP rate-limit (using the trustworthy IP, see H4), and cap prompt length. — `functions/index.js:1488-1564` **[confirmed]**\n- [ ] **C4 — Stop students self-grading.** Field-restrict `progress/{uid}` writes so students can write submission CONTENT but never `writtenScore`/`approved`/`correct`/`score`/`gradedBy`; compute MC correctness and embed scores server-side. — `firestore.rules:289-292` **[confirmed]**\n- [ ] **C5 — Stop client-trusted embed grades.** Add server-side authority: write raw interaction events to an append-only path, have a Cloud Function compute the score into a teacher-only-writable field; recompute embed scores in classroom-sync/autograde rather than trusting the self-reported number. (Root cause shared with C4.) — `LessonViewer.jsx:179-191`, `firestore.rules:289-292`, `classroom-sync.cjs:365-370` **[confirmed]**\n\n### HIGH\n- [ ] **H1 — Server-control enrollment.** Add an `enrollInCourse` Cloud Function that validates the enroll code; reject student edits to `enrolledCourses`. — `firestore.rules:64-67, 71-73`; `src/lib/enrollment.jsx:164-166` **[confirmed]**\n- [ ] **H2 — Scope session reads (PantherPrep).** `allow read: if isPapsUser() && (resource.data.uid == request.auth.uid || isStaff(...))`; `create` requires own uid. — `pantherprep/firestore.rules:21-24` **[confirmed]**\n- [ ] **H3 — Add server-side role guard to teacher routes.** Add a `<TeacherRoute>` wrapper AND ensure every teacher action is enforced by firestore.rules with a trustworthy role (depends on C1). — `src/App.jsx:89-101, 133-168` **[confirmed]**\n- [ ] **H4 — Fix spoofable IP rate-limit in papsAiBuildsProxy.** Use the LAST X-Forwarded-For entry (LB-appended) or `req.ip`, not the first; add App Check / server token; cap payload + output tokens; treat Origin as a hint not a control. — `functions/index.js:2882-2933` **[confirmed]**\n- [ ] **H5 — Unify grade calculation across all three surfaces.** Add `ranking`/`linked`/`sorting`/`concept_builder` handling to MyGrades and StudentProgress, mirroring classroom-sync; extract a shared `computeLessonGrade()` module + fixture unit test so the allowlist can't drift. (Also closes M-divergence finding on `nonEmbedPts`.) — `MyGrades.jsx:198-282`, `StudentProgress.jsx:371-518`, `classroom-sync.cjs:244-371` **[confirmed]**\n- [ ] **H6 — Fix EmbedBlock monotonic-score guard stranding lessons.** Separate score-monotonicity from the submitted-flag; always honor `gameComplete:true` to set `submitted`, even on a lower final score. — `EmbedBlock.jsx:107-116` **[confirmed]**\n- [ ] **H7 — Make safeLessonWrite fuzzy-match safe.** Preserve only exact-ID or exact-text matches; abort+report on ambiguity; remove positional fallback for question blocks; raise threshold to ~0.95; log every remap. — `scripts/safe-lesson-write.cjs:87-135, 100-104` **[confirmed]**\n- [ ] **H8 — Fix ConnectFour double-count.** Move the once-per-game guard INTO `handleGameEnd`; reset the ref in `handleBackToLobby`. — `ConnectFourBlock.jsx:294-298, 242, 302-309, 488-491` **[confirmed]**\n\n### MEDIUM\n- [ ] **M1 — Make mana balance server-authoritative.** Forbid student writes to `balance`/`lifetimeEarned`/`level`/`history`; route all spend/charge through Cloud Functions. — `firestore.rules:128-141` **[confirmed]**\n- [ ] **M2 — Scope user-profile reads.** `allow read: if isOwner(uid) || isTeacher()`; add narrow per-feature reads for roster/chat. — `firestore.rules:71-72` **[confirmed]**\n- [ ] **M3 — Lock studentProfiles/classSongs to teachers.** `allow read, write: if isTeacher()` (or a dedicated service identity). URL obscurity is not authz. — `firestore.rules:33-40` **[confirmed]**\n- [ ] **M4 — Don't broadcast raw ID token to iframes.** Mint a short-lived scoped custom token per blockId via Cloud Function; tighten `isAllowedOrigin` to exact hosts, drop the `*-paps` wildcard; XSS-audit embed tools. — `EmbedBlock.jsx:87-97` **[confirmed]**\n- [ ] **M5 — Validate activityScore payload.** Require `maxScore` to match the block's configured max; require explicit `gameComplete:true`; treat client score as a display hint. (Mitigated by C5.) — `EmbedBlock.jsx:99-120` **[confirmed]**\n- [ ] **M6 — Rate-limit queryRecipeBot.** Add per-uid 10/60s limit + prompt-length cap; narrow CORS. — `functions/index.js:1320-1397` **[confirmed]**\n- [ ] **M7 — Audit-log viewAsUid impersonation.** Write an audit entry (teacher uid, viewed student, lesson, ts) on each viewAsUid use; primary fix is C1. — `LessonViewer.jsx:28-39, 79-124` **[confirmed]**\n- [ ] **M8 — Stop email-pattern teacher grants.** Assign role server-side via explicit UID/email allowlist as a custom claim; confirm @lachlan.internal email/password self-signup is disabled. — `useAuth.jsx:33-44` **[confirmed]**\n- [ ] **M9 — Harden safeLessonWrite .set() path.** Assert remappedBlocks is a superset of all graded old IDs; abort on any drop without explicit flag; use `updateDoc` not full `.set()`; add dry-run/diff. — `scripts/safe-lesson-write.cjs:54-80` **[confirmed]**\n- [ ] **M10 — Compute adaptiveProfile server-side (PantherPrep).** Write mastery only from the immutable performanceLog via Cloud Function; set client write to `false`. — `pantherprep/firestore.rules:72-78` **[confirmed]**\n\n### LOW\n- [ ] **L1 — Scope pantherTrack reads to owning teacher.** Behavior notes/events/rosters must be teacher-only on read. — `firestore.rules:366-415` **[confirmed]**\n- [ ] **L2 — PantherPrep adaptiveProfile/studyPlans self-write.** Make server-authoritative if dashboards depend on it, else document as untrusted. — `pantherprep/firestore.rules:72-77, 138-153` **[confirmed]**\n- [ ] **L3 — Remove hardcoded quiz-unlock code in future tools.** Gate make-ups server-side; no change needed to the past-dated circuits quiz. — `public/tools/circuits-quiz/index.html:196, 527` **[confirmed]**\n- [ ] **L4 — Apply input-size caps to all AI proxies.** Cap studentPrompt/prompt/blocks length consistently (botChat pattern). — `functions/index.js:1488, 1320, 711` **[confirmed]**\n\n### INFO (no action required; confirm/maintain)\n- [ ] **I1 — Open `if true` rules on secretChat + JARVIS memories.** Intentional/isolated; add shape limits + TTL, confirm no plaintext PII. — `firestore.rules:7-28` **[confirmed]**\n- [ ] **I2 — All client AI inference routed through Cloud Function proxies.** No direct provider calls, no client-side keys. Correct architecture — maintain it. — `src/lib/api.jsx:5` **[confirmed]**\n\n---\n\n## 4. Findings Detail\n\n### CRITICAL\n\n#### C1 — Student self-promotes to teacher via own user doc (full privilege escalation)\n- **File:** `firestore.rules:71-73, 45-48`; also `storage.rules:5-9`, `functions/index.js:737-738, 969-970, 889-891`\n- **Category:** firestore-rules-authz / privilege-escalation · **[confirmed, 3/3]**\n- **What/why:** `users/{uid}` rule is `allow write: if isOwner(uid) || isTeacher()`, with no field-level restriction. A student owns their own doc, so they can write any field including `role:\"teacher\"`. `isTeacher()` derives teacher status entirely from `get(users/$(uid)).data.role == \"teacher\"` — the same self-writable field. There are no custom claims backing role; `getRoleFromEmail()` only seeds client state. Once set, every `isTeacher()`/`isCourseTeacher()` check across the whole ruleset (grades, PII, mana, Mission Control, agentContext, pantherTrack notes) evaluates true.\n- **Exploit:** Logged-in student in devtools runs `setDoc(doc(db,'users',myUid),{role:'teacher'},{merge:true})`. Passes (isOwner true). Next request, isTeacher() returns true — reads/overwrites every student's grades, reads all PII, edits any lesson (regenerating block IDs → can zero grades), grants mana. Total platform compromise from a student account, no social engineering.\n- **Fix:** Move role to a custom auth claim set only server-side; `isTeacher()` reads `request.auth.token.role`. Stopgap: `allow update: if isOwner(uid) && !request.resource.data.diff(resource.data).affectedKeys().hasAny(['role','enrolledCourses','isTestStudent'])`. Never let isOwner write its own role.\n\n#### C2 — Student self-escalates to teacher (PantherPrep) — all student PII exposed\n- **File:** `pantherprep/firestore.rules:16-19` (students write) + `158-162` (isStaff); client `contexts/auth-context.tsx:43-68`\n- **Category:** authorization / privilege escalation · **[confirmed, 3/3]**\n- **What/why:** `students/{uid}` allows `write: if request.auth.uid == uid` with no field allowlist; `isStaff(uid)` reads `students/{uid}.data.role`. Student controls that field → self-promotes. Then unlocks cross-student reads on `performanceLog`, `adaptiveProfile`, `coachThreads`, `coachNotes`, `studyPlans`, plus writes to `coachDrafts` and `questionPool`. FERPA-relevant breach: every classmate's scores, mastery, study plans, and private coach conversations.\n- **Exploit:** Signed-in student runs `firebase.firestore().doc('students/'+uid).set({role:'teacher'},{merge:true})`, then `collection('sessions').get()` / `collectionGroup('answers')` / reads any `coachNotes`. Exfiltrates everyone's data; can forge coachDrafts and overwrite questionPool.\n- **Fix:** `allow create: if request.auth.uid == uid && request.resource.data.role == 'student'`; `allow update: if request.auth.uid == uid && !request.resource.data.diff(resource.data).affectedKeys().hasAny(['role'])`. Better: custom claim, isStaff reads `request.auth.token.role`; move auth-context self-heal server-side.\n\n#### C3 — geminiProxy: fully public, unauthenticated, unthrottled LLM proxy\n- **File:** `functions/index.js:1488-1564`; URL hardcoded at `prompt-duel-firebase/src/lib/firebase.js:24`\n- **Category:** cloud-functions-authz · **[confirmed, 3/3]**\n- **What/why:** `cors:true`, NO auth, NO rate limit, NO origin check. Forwards caller's `studentPrompt` verbatim to gemini-2.5-flash (512 out tokens for `generate`; 2048 out + 1024 thinking for `judge`). No input length cap. Code comment literally says \"no auth required.\" Every sibling proxy requires a verified ID token; this one does not. URL is in deployed public JS.\n- **Exploit:** Anyone scripts `POST {action:'judge', payload:{...}}` in a loop from any host. With maxInstances:30 and judge's large token budget, thousands of arbitrary completions run on Luke's billed key — racking cost, exhausting shared quota (breaking geminiChat/botChat for real students mid-class), and serving as a free general LLM. No login, no IP cap stops them.\n- **Fix:** Require verified Firebase ID token + per-uid 10/60s rate limit (copy geminiChat/queryRecipeBot). If intentionally login-free: lock CORS to prompt-duel origin, add IP rate-limit (use trustworthy IP per H4), cap payload length. Auth strongly preferred.\n\n#### C4 — Students can self-grade: progress/{uid} write rule has no field validation\n- **File:** `firestore.rules:289-292`; `StudentProgress.jsx:382-461`\n- **Category:** grade-integrity / broken access control · **[confirmed, 3/3]**\n- **What/why:** Grades are computed client-side from `a.correct`, `a.writtenScore`, `a.score`, `a.submitted`, `a.approved` under `progress/{uid}/...answers.{blockId}`. The rule grants `allow write: if isOwner(uid) || isTeacher()` over the whole subtree with no field validation. A student can directly write `writtenScore:1`/`correct:true`/`submitted:true`/`approved:true` — including teacher_checkpoint approvals and short-answer scores meant to be teacher-only. Never recomputed server-side; classroom-sync reads these stored values.\n- **Exploit:** `setDoc(doc(db,'progress',uid,'courses',cid,'lessons',lid),{answers:{[blockId]:{submitted:true,writtenScore:1,correct:true,approved:true,score:5,maxScore:5}}},{merge:true})`. MyGrades and teacher StudentProgress show full credit; 4 PM sync pushes the inflated grade to Classroom. Teacher checkpoint approvals fully bypassable.\n- **Fix:** Field-restrict student writes to submission CONTENT only; teacher-owned fields (`writtenScore`, `writtenLabel`, `correct`, `approved`, `score` on checkpoints, `gradedBy`) writable only by `isCourseTeacher(courseId)`. Compute MC correctness server-side against the answer key, not trusting client `correct`.\n\n#### C5 — Embed scores client-authored, never re-verified server-side\n- **File:** `LessonViewer.jsx:179-191`; `firestore.rules:289-292`; `classroom-sync.cjs:365-370`; autograde `functions/index.js:1571, 1604`\n- **Category:** grade-security / broken access control · **[confirmed, 3/3]**\n- **What/why:** End-to-end client-trusted: EmbedBlock receives `activityScore` postMessage → `handleAnswer` writes to `progress/{uid}/...` → rule allows owner write → classroom-sync sums `a.writtenScore * pts` with NO re-grade (unlike MC, re-checked at L296). Autograde only fills `writtenScore` when null; never caps/recomputes embeds. The postMessage origin check is NOT a security boundary — it only stops third-party sites, not the authenticated student whose own browser and own Firestore doc are involved.\n- **Exploit:** `updateDoc(doc(db,'progress',myUid,'courses',cid,'lessons',lid),{['answers.'+embedBlockId]:{score:5,maxScore:5,writtenScore:1,submitted:true}})` — rules permit it. Or, even simpler, from the iframe console: `window.parent.postMessage({type:'activityScore',score:5,maxScore:5,gameComplete:true},'*')`. 100% with no work; pushed to Classroom at 4 PM. Works for every scored embed in every lesson.\n- **Fix:** Server-side authority — write raw interaction events to a student-write-once append-only path; a Cloud Function computes `writtenScore` into a teacher-only-writable field; classroom-sync/autograde recompute from interaction data. The whole-subtree `allow write: if isOwner(uid)` is the root hole. Document as a known grade-integrity gap until fixed.\n\n### HIGH\n\n#### H1 — Student self-enrolls into ANY course (bypasses enroll code)\n- **File:** `firestore.rules:64-67, 71-73`; `src/lib/enrollment.jsx:164-166` · **[confirmed, 3/3]**\n- **What/why:** `isEnrolledStudent(courseId)` trusts `courseId in get(users/$(uid)).data.enrolledCourses` — a self-writable map. The enroll-code check is client-side JS only; no rule/function validates the code before the map is written.\n- **Exploit:** `setDoc(doc(db,'users',myUid),{enrolledCourses:{'<otherCourseId>':true}},{merge:true})` → isEnrolledStudent passes. Read another teacher's lessons/announcements/chat, see/submit into a class never admitted to.\n- **Fix:** `enrollInCourse` Cloud Function validates the code (admin SDK) and writes the map; rule rejects student edits to `enrolledCourses`/`role`. Or store enrollment authoritatively in a server-written collection.\n\n#### H2 — Any authenticated user reads ALL practice/test sessions (PantherPrep)\n- **File:** `pantherprep/firestore.rules:21-24` · **[confirmed, 3/3]**\n- **What/why:** `sessions` rule is `allow read: if isPapsUser()` — every signed-in student can read every session (scores, answers, timestamps). Client filters by uid, but rules are the real boundary. Independent of escalation — no privileges needed.\n- **Exploit:** `firebase.firestore().collection('sessions').get()` returns all students' sessions.\n- **Fix:** `allow read: if isPapsUser() && (resource.data.uid == request.auth.uid || isStaff(request.auth.uid))`; `allow create: if request.resource.data.uid == request.auth.uid`.\n\n#### H3 — Teacher routes gated only in client UI (login-only, not role)\n- **File:** `src/App.jsx:89-101, 133-168`; `useAuth.jsx:26-45` · **[confirmed, 2/3]**\n- **What/why:** `ProtectedLayout` checks `if (!user)` but NOT role. Teacher routes (/editor, /grading, /rosters, /progress/*, /xp-controls, /mana, /teacher/*, dashboards) have no route-layer role guard; each page enforces role internally from client-derived `userRole`. The only real boundary is firestore.rules — which C1/C4 show is currently bypassable. Defense-in-UI only; creates a false sense of security.\n- **Exploit:** On its own a student just sees \"Teacher access only.\" But the UI gate is meaningless once role is spoofable (C1) or Firestore writes are called directly (C4) — no second server-side line of defense at the route/action layer.\n- **Fix:** Add a role-aware `<TeacherRoute>` wrapper AND enforce every teacher write in firestore.rules using a server-trusted role (custom claim).\n\n#### H4 — papsAiBuildsProxy IP rate-limit bypassed via spoofed X-Forwarded-For\n- **File:** `functions/index.js:2882-2933` · **[confirmed, 3/3]**\n- **What/why:** Unauthenticated by design. Abuse control = origin-lock + model-allowlist + 30/60s per-IP. IP is taken from `x-forwarded-for` first entry with no validation — client-controlled. Rotate a fake XFF per request → each lands in a unique rateLimits doc → cap never triggers. Origin header is also client-settable for non-browser requests. Only real protection left is the model allowlist. Also balloons Firestore with one doc per spoofed IP.\n- **Exploit:** Loop with `Origin: https://paps-ai-builds.web.app` + `X-Forwarded-For: 10.0.0.$i`, body `{model:'gemini-2.5-pro', payload:{long prompt}}`. Origin passes (forged), unique XFF defeats cap, most-expensive model runs unbounded on Luke's key; creates 100k junk docs.\n- **Fix:** Trust only the platform IP — use the LAST XFF entry (LB appends real client IP) or `req.ip`. Add App Check / server-issued token; cap payload + output tokens. Keep model allowlist; treat Origin as a hint.\n\n#### H5 — Graded block types counted by classroom-sync but dropped by MyGrades & StudentProgress\n- **File:** `MyGrades.jsx:198-282`; `StudentProgress.jsx:371-518`; `classroom-sync.cjs:244-371` · **[confirmed, 3/3]**\n- **What/why:** classroom-sync grades MC, SA, `ranking` (partialScore, L307-312), `linked` (writtenScore, L315-320), `sorting` (L323-329), `concept_builder` (L332-337), teacher_checkpoint, embeds, dropdown data_tables. Both student-facing calculators count only a SUBSET (mc + sa + embeds + teacher_checkpoint + dropdown data_table) — no case for ranking/linked/sorting/concept_builder. These types are widespread across physics seeds (energy-skate-park, elastic-PE, intro-to-energy, transformations, conservation-bar-charts; `linked` in 10+ seeds; `sorting` in 10+). Data IS captured and Classroom uses it — but the student never sees those points and the teacher's gradebook computes a DIFFERENT grade than what synced.\n- **Exploit:** Lesson with 2 MC + 1 ranking + 1 linked: student gets 2/2 MC, ranking 0.5, linked 1.0. classroom-sync = 3.5/4 = 88% → Classroom. MyGrades + StudentProgress = 2/2 = 100%. Three different grades for the same work; the gradebook of record silently disagrees with what students and teacher see.\n- **Fix:** Add ranking/linked/sorting/concept_builder to BOTH app calculators, mirroring classroom-sync exactly. Better: extract one shared `computeLessonGrade()` imported by all three; add a fixture unit test asserting identical earned/possible.\n\n#### H6 — EmbedBlock monotonic score guard strands lessons in \"Not Submitted\"\n- **File:** `EmbedBlock.jsx:107-116`; `classroom-sync.cjs:368` · **[confirmed, 2/3]**\n- **What/why:** L107 early-returns when `newWrittenScore < existing.writtenScore` — BEFORE the `onAnswer` that sets `submitted` (L116). So `submitted:true` is only written when the new score ≥ existing. An interim high score (`gameComplete:false`) followed by a lower FINAL run (`gameComplete:true`) drops the final message entirely — the lesson never flips submitted. MyGrades requires submitted; classroom-sync skips `if (!a?.submitted)`. Student finished, sees a correctly-stored lower score, but is stuck \"Not Submitted,\" Complete Lesson never unlocks, gradebook gets nothing. Triggered by the exact interim-save behavior the scored-embed-checklist recommends.\n- **Exploit:** Tool posts `{score:5,maxScore:5,gameComplete:false}` after round 1 (writtenScore=1.0), then final `{score:3,maxScore:5,gameComplete:true}`. 0.6 < 1.0 → final dropped → submitted never set. Silent grade loss.\n- **Fix:** Separate score-monotonicity from the submitted flag. Always honor `gameComplete:true` to set `submitted` even on a lower score; keep the higher score for display but never early-return past the submitted write when gameComplete is true.\n\n#### H7 — safeLessonWrite fuzzy content-match silently rebinds answers to the wrong question\n- **File:** `scripts/safe-lesson-write.cjs:87-135, 100-104` · **[confirmed, 2/3]**\n- **What/why:** On re-seed of a lesson with progress, `findBestContentMatch` accepts any match with `textSimilarity >= 0.6` (crude shared-word ratio). Two distinct MC questions sharing boilerplate stems (\"Which of the following best describes…\") can exceed 0.6. The new block then inherits the old block's ID, and every student's stored answer grades against a different question. Because the ID still matches, classroom-sync's orphan guard (fires only when ZERO IDs match) and MyGrades detect nothing — the wrong grade is computed silently. The positional fallback (L68-73) can also rebind to an unrelated same-type block with zero textual match.\n- **Exploit:** A lesson edited to fix a typo + add one MC is re-seeded; two stems share ~65% words. safeLessonWrite swaps Q3/Q4 IDs. Students who answered old Q3 correctly are now graded against new Q3's different correct option — correct flips to incorrect for an unknown number of students, no error, no warning.\n- **Fix:** Preserve only exact-ID or exact-text matches; ABORT + report on anything else. Remove the positional fallback for question blocks; raise threshold to ~0.95; refuse to remap when two old same-type blocks are within threshold of one new block. Log every remap with old/new prompt text. Best: require seed scripts to carry stable explicit block IDs.\n\n#### H8 — ConnectFour double-counts game completion (grade inflation)\n- **File:** `ConnectFourBlock.jsx:294-298 (handleGameEnd), 242 (AI path), 302-309 (subscription), 488-491 (answer path)` · **[confirmed, 3/3]**\n- **What/why:** `handleGameEnd` does `setGamesCompleted(gamesCompleted + 1)` with no own guard. Called from three places: the subscription effect (guarded by `lastCountedGameRef`), the AI-move effect (L242, NO ref guard), and the answer path (guarded). When a game ends on the AI's move, L242 increments (N+1) without touching the ref; the Firestore subscription then sees status:'finished', `lastCountedGameRef.current !== activeGameId`, and calls handleGameEnd AGAIN (N+2). `getScore()` maps ≥3 games → 5/5, so the inflated count is sent as the grade.\n- **Exploit:** AI lands the final move → count=1 (L242) then count=2 (subscription). Two such games → count=4 → 5/5. Full credit for ~2 games instead of the required 3.\n- **Fix:** Move the once-per-game guard INTO `handleGameEnd` (`if (lastCountedGameRef.current === activeGameId) return; lastCountedGameRef.current = activeGameId; ...`), remove redundant call-site checks, and reset the ref in `handleBackToLobby`.\n\n### MEDIUM\n\n#### M1 — Student can set own mana balance to any value\n- **File:** `firestore.rules:128-141`; `src/lib/mana.jsx` · **[confirmed, 1/1]**\n- **What/why:** `studentMana/{uid}` allows `write: if isCourseTeacher(courseId) || request.auth.uid == uid`. The comment assumes crediting goes through Cloud Functions with caps, but the rule lets a student write any field — including `balance` directly. The transactions ledger is likewise self-writable (forge history).\n- **Exploit:** `setDoc(doc(db,'courses',cid,'studentMana',myUid),{balance:999999},{merge:true})`, then redeem real-world perks never earned. CF caps are irrelevant — the direct write bypasses them.\n- **Fix:** Make `balance`/`lifetimeEarned`/`level`/`history` server-authoritative (CF only); student writes nothing economic here. Same for the transactions subcollection.\n\n#### M2 — Any authenticated user reads every user profile (PII)\n- **File:** `firestore.rules:71-72` · **[confirmed, 1/1]**\n- **What/why:** `users/{uid}` `allow read: if isLoggedIn()`. Any student reads EVERY user doc school-wide (name, email, role, enrolledCourses, profile). No course-scoping.\n- **Exploit:** Query the whole users collection from devtools → names + emails of every student and teacher, plus enrollments. FERPA-relevant.\n- **Fix:** `allow read: if isOwner(uid) || isTeacher()`; add narrow per-feature reads where cross-student names are genuinely needed (course chat members).\n\n#### M3 — studentProfiles / classSongs readable & writable by any authed account\n- **File:** `firestore.rules:33-40` · **[confirmed, 1/1]**\n- **What/why:** Both use `allow read, write: if request.auth != null`. Intended as Lachlan-internal obscure-URL UI with teacher notes about students for song generation — but PantherLearn students authenticate too, so every student can read/overwrite all those notes and drafts. URL obscurity ≠ authz.\n- **Exploit:** List `studentProfiles` from devtools, read teacher notes about classmates (possibly behavioral/personal), or wipe them.\n- **Fix:** `allow read, write: if isTeacher()`; if the generator runs anonymously, give it a dedicated service identity/claim.\n\n#### M4 — EmbedBlock leaks student Firebase ID token to any allowed-origin iframe\n- **File:** `EmbedBlock.jsx:87-97` · **[confirmed, 1/1]**\n- **What/why:** On `requestAuthToken`, EmbedBlock calls `user.getIdToken()` and posts the real Firebase credential back to the iframe. The origin allowlist is broad (`*-paps.web.app`/`firebaseapp.com`, plus paps-tools/pantherlearn/pantherprep/brstatus hosts). Any XSS-able embed tool, or any future tool deployed to a `-paps` site, can harvest live tokens and act as the student (read/write grades) for ~1 hour.\n- **Exploit:** An embed with a reflected-XSS sink runs `parent.postMessage({type:'requestAuthToken'},'*')`, receives the token, exfiltrates it, then calls Firestore as the student to rewrite grades.\n- **Fix:** Don't broadcast the raw token. Prefer same-origin tools; or mint a short-lived custom token scoped to the specific blockId via a Cloud Function. Tighten `isAllowedOrigin` to exact hosts; drop the `*-paps` wildcard; XSS-audit every embed.\n\n#### M5 — activityScore payload has no integrity binding; gameComplete/maxScore attacker-controlled\n- **File:** `EmbedBlock.jsx:99-120` · **[confirmed, 1/1]**\n- **What/why:** Minimal validation: `maxScore` defaults to 100 if absent; `score`/`maxScore` taken verbatim; `gameComplete` defaults true (`!== false`). So any message with `type:'activityScore'` + a `score` flips `submitted:true` and locks the grade. Nothing ties the score to actual completion or to the block's expected max.\n- **Exploit:** From the genuine iframe console: `parent.postMessage({type:'activityScore',score:1,maxScore:1},'*')` → writtenScore=1, submitted true, perfect grade, no work. `score:1/maxScore:1` sidesteps any expectation that maxScore = the embed's real total.\n- **Fix:** Mitigated by C5. Defense-in-depth: require `maxScore` present and matching the block's configured max; require explicit `gameComplete:true`; accept only a server-verifiable artifact; treat the client number as a display hint.\n\n#### M6 — queryRecipeBot authenticated but has no rate limit\n- **File:** `functions/index.js:1320-1397` · **[confirmed, 1/1]**\n- **What/why:** Verifies an ID token (good) but, unlike geminiChat/botChat/botEmbed, has NO per-uid rate limit and no input length cap. Forwards `prompt` to gemini-2.5-flash-lite on every call; `cors:true`.\n- **Exploit:** A signed-in student (or a phished token) loops calls — hundreds of generateContent/min, draining quota and degrading class-wide AI. Lower than geminiProxy since it needs a valid account.\n- **Fix:** Add per-uid 10/60s limit (rateLimits/{uid}), cap prompt length (~2000 chars) and selectedSources count; narrow CORS.\n\n#### M7 — viewAsUid impersonation correctly gated but depends on spoofable role; no audit log\n- **File:** `LessonViewer.jsx:28-39, 79-124`; `firestore.rules:290` · **[confirmed, 1/1]**\n- **What/why:** `viewAsUid` is honored only when isTeacher, and progress reads require `isOwner || isTeacher` — so a plain student CANNOT read a peer's submissions via the URL param today. NOT a standalone horizontal hole. But it's entirely dependent on isTeacher() being trustworthy (C1), and there's no audit log when a teacher opens a student's full submissions (block_reset is logged, viewAs is not).\n- **Exploit:** A student who self-escalated (C1) appends `?viewAsUid=<peerUid>` to any lesson URL and reads peer answers/chat read-only across every course. Without escalation, the read is denied.\n- **Fix:** Primary fix is C1. Additionally write an audit entry (teacher uid/email, viewed student uid, lesson, ts) on each viewAsUid use, mirroring the resetLog pattern.\n\n#### M8 — Teacher role granted to any @lachlan.internal email from client pattern\n- **File:** `useAuth.jsx:33-44` · **[confirmed, 1/1]**\n- **What/why:** `getRoleFromEmail` grants teacher to any `@lachlan.internal` and to any @paps.net local-part with <3 digits — client-side only, seeded into `users.role`. With email/password enabled for QA, anyone who can create such an auth user is seeded teacher on first login and thereafter passes every isTeacher() rule. The digit heuristic is also brittle (mis-classifies teachers with 3+ digits, students with short names).\n- **Exploit:** If email/password self-signup is open, register `qa@lachlan.internal`, log in, get auto-seeded `role:'teacher'`, gain full teacher access.\n- **Fix:** Assign role server-side via an explicit teacher UID/email allowlist as a custom claim; treat @lachlan.internal as a narrow allowlist entry, not a wildcard suffix. Confirm/disable open registration for teacher-granting domains.\n\n#### M9 — safeLessonWrite uses raw .set() on has-progress path; unmatched blocks orphan grades\n- **File:** `scripts/safe-lesson-write.cjs:54-80` · **[confirmed, 1/1]**\n- **What/why:** On the progress-preserving path, a block with no match returns the NEW block unchanged (new ID), then `lessonRef.set(updatedLesson)` does a full overwrite. A re-seed that adds/reorders blocks so one graded block fails to match writes a brand-new ID for it — orphaning every student's answer for that block. Partial silent orphan (most IDs kept, one regenerated), easy to miss. Also contradicts the \"append-only\" promise in grade-data-integrity.md — reordering/removal in `newLesson.blocks` is faithfully written.\n- **Exploit:** Re-seed of an AI Lit lesson drops/renames an old embed block; students keep answers under the old ID, the new doc has a different-ID block; old answers orphan (classroom-sync skips only if ALL match), grades shift down silently.\n- **Fix:** Verify EVERY old block ID with student answers is present in remappedBlocks before writing; abort + report on any drop. Use `updateDoc` with an explicit blocks field, not full `.set()`; add a dry-run/diff and refuse to write on any drop without `--force-orphan`.\n\n#### M10 — Students can forge their own adaptive mastery profile (PantherPrep)\n- **File:** `pantherprep/firestore.rules:72-78`; `lib/adaptive/performance-service.ts` · **[confirmed, 1/1]**\n- **What/why:** `adaptiveProfile/{uid}` allows `write: if request.auth.uid == uid` with no shape validation. The engine treats this as computed mastery; a student can write arbitrary mastery values, defeating adaptive logic and corrupting teacher dashboards that aggregate up to 200 profiles. The immutable per-answer `performanceLog` is correctly locked — this is the weak link.\n- **Exploit:** Write `{mastery:{all_skills:1.0}}` → dashboard shows false mastery; engine stops surfacing weak-skill practice.\n- **Fix:** Compute/write adaptiveProfile server-side from the immutable performanceLog (CF/scheduled job); set client `allow write: if false`. If a client write must remain, constrain with `affectedKeys().hasOnly([...])` + value bounds.\n\n### LOW\n\n#### L1 — pantherTrack class read open to any authed user (cross-teacher behavior data)\n- **File:** `firestore.rules:366-415` · **[confirmed, 1/1]**\n- **What/why:** `pantherTrack/classes/list/{classId}` and ALL subcollections (students, events, deputies, rewards, redemptions, notes) grant `allow read: if isLoggedIn()`. The notes subcollection is described as teacher-only but its read is just isLoggedIn(). Any student reads every teacher's rosters, event logs, and private behavior notes. `create` is also open.\n- **Exploit:** Enumerate `pantherTrack/classes/list` and read behavior notes/event histories about self and classmates across all teachers — sensitive disciplinary PII.\n- **Fix:** `allow read: if isLoggedIn() && resource.data.teacherUid == request.auth.uid` on the class doc; gate each subcollection read on the parent teacherUid (as writes already do). Notes especially teacher-only.\n\n#### L2 — PantherPrep adaptiveProfile/studyPlans self-writable (low grade impact)\n- **File:** `pantherprep/firestore.rules:72-77, 138-153` · **[confirmed, 1/1]**\n- **What/why:** Student can write own mastery/studyPlans (studyPlans via affectedKeys allowlist). PantherPrep is a free, ungraded prep tool, so impact is skewed adaptive recommendations + dashboard accuracy, not a real grade. performanceLog is correctly immutable.\n- **Exploit:** Overwrite adaptiveProfile to mark all skills mastered, gaming the dashboard/coach triggers. No grade affected.\n- **Fix:** If dashboards/coach triggers depend on it, make mastery fields server-authoritative; otherwise document as self-reported/untrusted.\n\n#### L3 — Hardcoded client-side quiz-unlock bypass code in circuits-quiz\n- **File:** `public/tools/circuits-quiz/index.html:196, 527` · **[confirmed, 1/1]**\n- **What/why:** Hardcodes `MAKEUP_CODE = \"p1unlock\"` in shipped client JS; typing it opens the timed quiz outside its window. Not an API secret or grade-spoof vector (answers still scored), but a credential in client code. Low: quiz window already passed (TARGET_DATE 2026-05-21); gate is convenience, not security.\n- **Exploit:** View source → read `p1unlock` → start/restart the timed quiz outside the proctored window.\n- **Fix:** For future timed quizzes, gate make-up access server-side (CF verifies a teacher-issued one-time token or checks role/uid against an allowlist). No change needed to this past-dated quiz.\n\n#### L4 — AI proxies forward prompts with no input-size caps (quota/cost amplification)\n- **File:** `functions/index.js:1488, 1320, 711` · **[confirmed, 1/1]**\n- **What/why:** geminiProxy (studentPrompt), queryRecipeBot (prompt), generateQuestion (blocks) forward caller text with no max length. botChat/botEmbed DO cap (systemPrompt ≤2000, messages ≤50) — pattern exists but isn't applied consistently. Large inputs multiply per-call cost and amplify the rate-limit gaps above.\n- **Exploit:** Send max-size bodies on every call, maximizing billed input tokens and accelerating quota exhaustion.\n- **Fix:** Apply consistent caps everywhere prompts reach an LLM (max length ~2000-4000 chars, max array counts), mirroring botChat/botEmbed.\n\n### INFO\n\n#### I1 — Open `if true` rules on secretChat + JARVIS memories (by design)\n- **File:** `firestore.rules:7-28` · **[confirmed, 1/1]**\n- **What/why:** secretChatSignaling, secretChatRooms, and paps-ai-builds JARVIS memories use `allow read, write: if true`. Justified (ephemeral WebRTC signaling, AES-GCM ciphertext, no-PII memory keyed by localStorage UUID), isolated from grade/PII data. But anyone on the internet can read/write/enumerate/flood these.\n- **Exploit:** Enumerate secretChatRooms, harvest ciphertext + room codes, flood the collection (cost/abuse).\n- **Fix:** Keep open only if required for the auth-less build site; add shape/size limits, a TTL + scheduled cleanup, and confirm no plaintext identifiers. Prefer anonymous auth + `if request.auth != null` if the tool can sign in.\n\n#### I2 — All client AI inference routed through Cloud Function proxies (correct)\n- **File:** `src/lib/api.jsx:5` and related · **[confirmed, 1/1]**\n- **What/why:** Every AI/LLM call targets a `*.cloudfunctions.net` proxy, never a provider endpoint directly (geminiChat, botChat/botEmbed, generateQuestion, generateBaselines, validateReflection, translateText). Grep for generativelanguage/openai/anthropic/x-goog-api-key/`key=AIza` across public/tools and src = zero direct calls. No provider key client-side; auth via dynamic Firebase ID token. PantherPrep makes no direct AI calls.\n- **Fix:** None — correct architecture. Maintain it for every new tool/embed (never embed a provider key in HTML/JS).",
    "confirmed": [
      {
        "title": "Student can self-promote to teacher by writing role:\"teacher\" to own user doc (full privilege escalation)",
        "severity": "critical",
        "category": "firestore-rules-authz",
        "file": "/Users/lukemccarthy/pantherlearn/firestore.rules",
        "line": "71-73, 45-48",
        "description": "The users collection rule is `allow write: if isOwner(uid) || isTeacher()`. A student owns their own user doc, so they can write ANY field to it — including `role: \"teacher\"`. The `isTeacher()` helper (line 45-48) derives teacher status entirely from `get(users/$(uid)).data.role == \"teacher\"`. There is no separation between the self-writable profile field and the privilege field. Once a student sets their own role to teacher, every `isTeacher()` / `isCourseTeacher()` check across the entire ruleset evaluates true for them: they can read/write any course's lessons, every student's grades/reflections/responses, mana balances, Mission Control, agentContext, assignmentQueue, pantherTrack behavior notes, and all PII.",
        "evidence": "function isTeacher() {\n  return isLoggedIn()\n    && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == \"teacher\";\n}\n...\nmatch /users/{uid} {\n  allow read: if isLoggedIn();\n  allow write: if isOwner(uid) || isTeacher();",
        "exploit_scenario": "A logged-in student opens devtools and runs `setDoc(doc(db,'users',myUid), {role:'teacher'}, {merge:true})`. The write passes because isOwner(uid) is true. On the next request, isTeacher() returns true for them. They now read every student's grades via `courses/{c}/grades/{g}` (teacher branch), overwrite grades, read all PII in the users collection, and access teacher-only Mission Control. Total compromise of the grade-critical platform.",
        "proposed_fix": "Restrict the self-write so students cannot set/change privileged fields. Either: (a) `allow update: if isOwner(uid) && !request.resource.data.diff(resource.data).affectedKeys().hasAny(['role','enrolledCourses']) ` plus a separate teacher-only branch for role changes; or (b) move `role` to a custom auth claim (set only via admin SDK / Cloud Function) and have isTeacher() read `request.auth.token.role == 'teacher'` instead of the self-writable Firestore field. Custom claim is the robust fix since it also closes the related enrolledCourses self-grant below.",
        "dimension": "firestore-rules-authz",
        "confirmCount": 3,
        "verifierCount": 3
      },
      {
        "title": "geminiProxy is a fully public, unauthenticated, unthrottled LLM proxy — anyone can drain Luke's paid Gemini quota and run arbitrary prompts",
        "severity": "critical",
        "category": "cloud-functions-authz",
        "file": "/Users/lukemccarthy/pantherlearn/functions/index.js",
        "line": "1488-1564",
        "description": "exports.geminiProxy is configured with `cors: true` (any origin), has NO authentication check whatsoever (no Bearer token, no admin.auth().verifyIdToken, no role check), and NO rate limiting of any kind. Every other AI proxy in this file (geminiChat, botChat, botEmbed, validateReflection, queryRecipeBot) requires a verified Firebase ID token and most enforce a 10-req/60s per-uid limit. geminiProxy does not. It accepts `{action, payload}` where action='generate' forwards the caller's `studentPrompt` verbatim as the user prompt to gemini-2.5-flash with up to 512 output tokens, and action='judge' allows up to 2048 output tokens + 1024 thinking budget. The function is deployed and its URL is hardcoded in a public client: prompt-duel-firebase/src/lib/firebase.js line 24 = `https://us-central1-pantherlearn-d6f7c.cloudfunctions.net/geminiProxy`. There is no input length cap on studentPrompt either. The code comment even states 'no auth required'.",
        "evidence": "exports.geminiProxy = onRequest({ cors: true, secrets: [geminiApiKey], maxInstances: 30, timeoutSeconds: 60 }, async (req, res) => {\n  if (req.method !== \"POST\") return res.status(405).json({ error: \"POST only\" });\n  const { action, payload } = req.body;\n  if (!action || !payload) return res.status(400).json({ error: \"Missing action or payload\" });\n  ... // NO auth, NO rate limit, NO origin check\n  if (action === \"generate\") { ... userPrompt = studentPrompt; ... }\n  const url = `...generateContent?key=${geminiApiKey.value()}`;\n  const data = await callGeminiWithRetry(url, body);",
        "exploit_scenario": "Anyone who finds the URL (it's in deployed public JS at paps-/prompt-duel) writes a 5-line script that POSTs {action:'judge', payload:{...}} in a loop from any host. With maxInstances:30 and judge allowing 2048 out + 1024 thinking tokens per call, an attacker runs thousands of arbitrary LLM completions on Luke's billed Gemini key — racking up cost, exhausting the shared quota (which also breaks geminiChat/botChat for real students mid-class), and using the proxy as a free general-purpose LLM. No login, no class-period gate, no IP cap stops them.",
        "proposed_fix": "Require a verified Firebase ID token (copy the Bearer-token verifyIdToken block used by geminiChat/queryRecipeBot) AND add the same per-uid transactional rate limit (10/60s). If the Prompt Duel tool is intentionally login-free, at minimum: (1) lock CORS to the prompt-duel origin only (replace `cors: true` with the explicit origin array like translateText/papsAiBuildsProxy do), (2) add the IP-based transactional rate limit pattern used by papsAiBuildsProxy, and (3) cap studentPrompt/payload length. Auth is strongly preferred — students already sign into PantherLearn.",
        "dimension": "cloud-functions-authz",
        "confirmCount": 3,
        "verifierCount": 3
      },
      {
        "title": "Student can self-escalate to teacher by writing role:\"teacher\" to their own users doc",
        "severity": "critical",
        "category": "auth-role-gating / privilege-escalation",
        "file": "/Users/lukemccarthy/pantherlearn/firestore.rules",
        "line": "71-73 (users), 45-48 (isTeacher), and storage.rules 5-9",
        "description": "Role is the single source of truth for every teacher gate in the app, and it lives as a plain field in users/{uid}.data.role. firestore.rules isTeacher() = get(users/$(uid)).data.role == 'teacher'; storage.rules isTeacher() reads the same field; Cloud Functions (functions/index.js lines 737-738, 969-970, 889-891) also gate on userDoc.data().role === 'teacher'. But the write rule for the users doc is `allow write: if isOwner(uid) || isTeacher();` (line 73) with NO field-level restriction. A student IS the owner of their own users doc, so they can write {role:\"teacher\"} to it directly via the Firebase JS SDK. There are no Firebase custom claims backing role anywhere — getRoleFromEmail() in useAuth.jsx only sets client state and seeds the doc on first login; nothing re-asserts the email-derived role on the server after creation. syncUserProfile only prevents teacher->student downgrade, not student->teacher self-write outside the app.",
        "evidence": "firestore.rules:71-73  match /users/{uid} { allow read: if isLoggedIn(); allow write: if isOwner(uid) || isTeacher(); }\nfirestore.rules:45-48  function isTeacher(){ return isLoggedIn() && get(.../users/$(request.auth.uid)).data.role == \"teacher\"; }\nstorage.rules:6-9  function isTeacher(){ return ... firestore.get(.../users/$(request.auth.uid)).data.role == 'teacher'; }",
        "exploit_scenario": "A student opens devtools on pantherlearn.com (already authenticated), runs `setDoc(doc(db,'users',auth.currentUser.uid),{role:'teacher'},{merge:true})`. Every isTeacher() check now passes for them. They can then read/write ALL students' grades (progress/{anyUid}), edit any lesson (which regenerates block IDs and can zero grades), grant mana, read every student's submissions/chat logs/reflections, and read/write Mission Control + agentInbox data. Full teacher compromise from a student account, no social engineering.",
        "proposed_fix": "Move role to a Firebase Auth custom claim set ONLY by a trusted server (Cloud Function deriving role from the @paps.net email pattern + override lists), and change isTeacher() in both firestore.rules and storage.rules to read request.auth.token.role == 'teacher'. As an immediate hardening before that migration, restrict the users write rule so a non-teacher cannot change protected fields: e.g. `allow update: if isOwner(uid) && !request.resource.data.diff(resource.data).affectedKeys().hasAny(['role','isTestStudent','enrolledCourses']);` and allow role changes only via isTeacher() of a DIFFERENT user / a function. Never let isOwner write its own role.",
        "dimension": "auth-role-gating",
        "confirmCount": 3,
        "verifierCount": 3
      },
      {
        "title": "Students can self-grade: progress/{uid} write rule lets a student write arbitrary score fields to their own progress doc",
        "severity": "critical",
        "category": "auth-role-gating / grade-integrity",
        "file": "/Users/lukemccarthy/pantherlearn/firestore.rules",
        "line": "289-292",
        "description": "Grades are computed client-side in StudentProgress.getStudentLessonGrade() and MyGrades directly from the fields a.correct, a.writtenScore, a.score, a.submitted stored under progress/{uid}/courses/{c}/lessons/{l}.answers.{blockId} (StudentProgress.jsx:382-461). Those exact fields are what teacher grading writes (GradingDashboard writtenScore writes; StudentProgress approveCheckpoint at 293-334; ShowMeGrader setDoc at 147/181). The Firestore rule for that path is `match /progress/{uid}/{document=**} { allow write: if isOwner(uid) || isTeacher(); }` with no field validation. A student is the owner, so they can directly write writtenScore:1 / correct:true / submitted:true / score:weight / approved:true for any block — including teacher_checkpoint approvals and short-answer scores that are supposed to be teacher-only. The grade is never recomputed server-side; classroom-sync reads these same stored values.",
        "evidence": "firestore.rules:289-292  match /progress/{uid}/{document=**} { allow read: if isOwner(uid) || isTeacher(); allow write: if isOwner(uid) || isTeacher(); }\nStudentProgress.jsx:385  mcItems.push({... points: a.correct ? 1 : 0 ...})\nStudentProgress.jsx:395-397  if (a?.submitted && a.writtenScore != null) saItems.push({ points: a.writtenScore ...})\nStudentProgress.jsx:443  else if (a?.approved === true) checkpointItems.push({ points: weight, approved:true })",
        "exploit_scenario": "A student runs in devtools: setDoc(doc(db,'progress',uid,'courses',courseId,'lessons',lessonId),{answers:{[blockId]:{submitted:true,writtenScore:1,correct:true,approved:true,score:5,maxScore:5}}},{merge:true}). Their MyGrades and the teacher's StudentProgress both now show full credit; the 4 PM classroom-sync pushes the inflated grade to Google Classroom. Teacher checkpoint approvals (manual gate) are fully bypassable.",
        "proposed_fix": "Field-restrict student writes to progress. Students should be able to write submission CONTENT (answer text, selected option, completed flag) but NOT the graded outputs. Split the path so teacher-owned fields (writtenScore, writtenLabel, correct for MC should be server-derived, approved, score on checkpoints, gradedBy) are only writable by isCourseTeacher(courseId). Practically: validate with request.resource.data.diff in the rule to block students from setting writtenScore/approved/correct/score, and compute MC correctness via a Cloud Function or by comparing against the lesson answer key server-side rather than trusting client-written `correct`.",
        "dimension": "auth-role-gating",
        "confirmCount": 3,
        "verifierCount": 3
      },
      {
        "title": "Student can self-escalate to teacher role, exposing all students' PII (sessions, performance logs, study plans, coach notes)",
        "severity": "critical",
        "category": "authorization / privilege escalation",
        "file": "/Users/lukemccarthy/pantherprep/firestore.rules",
        "line": "16-19 (students write rule) + 158-162 (isStaff helper)",
        "description": "The `students/{uid}` rule allows `write: if request.auth != null && request.auth.uid == uid;` with NO field restriction. A student can therefore write any field to their own student doc, including `role`. The `isStaff(uid)` helper (lines 158-162) grants staff privileges by reading `students/{uid}.data.role == 'teacher' || 'admin'`. Since the student controls that exact field, any student can self-promote to teacher. Once `role:'teacher'` is set, `isStaff(request.auth.uid)` returns true and unlocks cross-student reads on `performanceLog/{uid}/answers` (l.63-66), `adaptiveProfile/{uid}` (l.73-76), `coachThreads/{uid}` (l.97-101), `coachNotes` (l.108-111), `studyPlans/{planId}` (l.139-142), plus WRITE access to `coachDrafts` (l.122-125) and `questionPool` (l.81-84). This is a full FERPA-relevant data breach: a student can read every other student's scores, mastery state, study plans, and private coach conversations, and tamper with the coach-draft queue and question bank. The client (contexts/auth-context.tsx l.43-68) reads this same role field to render the teacher dashboard, so the escalation also lights up the teacher UI.",
        "evidence": "match /students/{uid} {\n  allow read: if isPapsUser();\n  allow write: if request.auth != null && request.auth.uid == uid;  // no field allowlist — role is writable\n}\n...\nfunction isStaff(uid) {\n  return exists(/databases/$(database)/documents/students/$(uid)) &&\n    (get(/databases/$(database)/documents/students/$(uid)).data.role == \"teacher\" ||\n     get(/databases/$(database)/documents/students/$(uid)).data.role == \"admin\");\n}",
        "exploit_scenario": "A student opens devtools on pantherprep.web.app while signed in, runs: `firebase.firestore().doc('students/'+uid).set({role:'teacher'},{merge:true})`. The next `isStaff()` check passes. They then query `firebase.firestore().collection('sessions').get()` / `collectionGroup('answers')` / `collection('studyPlans').get()` / read any `coachNotes`, exfiltrating every classmate's scores, mastery, study plans and private coach messages. They can also write fake `coachDrafts` and overwrite `questionPool` entries.",
        "proposed_fix": "Do NOT let the role field be client-writable. Restrict the students write rule to forbid changing role, e.g.: `allow update: if request.auth.uid == uid && (!request.resource.data.diff(resource.data).affectedKeys().hasAny(['role']));` and `allow create: if request.auth.uid == uid && request.resource.data.role == 'student';`. Better: store role in a custom auth claim set only by admin SDK / a Cloud Function, and have isStaff()/isPapsUser() read `request.auth.token.role` instead of a self-writable Firestore field. The auth-context.tsx self-heal `setDoc({role: resolved})` (l.55-58) must then move server-side (it currently lets the client persist its own role).",
        "dimension": "pantherprep-audit",
        "confirmCount": 3,
        "verifierCount": 3
      },
      {
        "title": "Student can self-enroll into ANY course (bypass enroll code) by writing enrolledCourses on own user doc",
        "severity": "high",
        "category": "firestore-rules-authz",
        "file": "/Users/lukemccarthy/pantherlearn/firestore.rules",
        "line": "64-67, 71-73",
        "description": "isEnrolledStudent(courseId) trusts `courseId in get(users/$(uid)).data.enrolledCourses`. That enrolledCourses map lives on the student's own user doc, which they can write freely (allow write: if isOwner(uid)). The enroll-code check (src/lib/enrollment.jsx enrollWithCode) is purely client-side JS — there is no Cloud Function or rule that validates the code before the map is written. A student can add any courseId to their own enrolledCourses and gain enrolled access without ever knowing the code.",
        "evidence": "function isEnrolledStudent(courseId) {\n  return isLoggedIn()\n    && courseId in get(/databases/$(database)/documents/users/$(request.auth.uid)).data.enrolledCourses;\n}\n// enrollment.jsx line 164-166: client writes the map directly\nawait setDoc(userRef, { enrolledCourses: { ...currentCourses, [course.id]: true } }, { merge: true });",
        "exploit_scenario": "A student in Physics wants into another teacher's AI Lit course. In devtools: `setDoc(doc(db,'users',myUid), {enrolledCourses:{'<otherCourseId>':true}}, {merge:true})`. isEnrolledStudent now passes for that course. They can read all its lessons, announcements, chat, and — for game/reflection paths keyed only by hasCourseAccess — see other students' game state and submit data into a class they were never admitted to.",
        "proposed_fix": "The enrollment grant must be server-controlled. Add an `enrollInCourse` Cloud Function that validates the enroll code against the course doc (admin SDK) and writes enrolledCourses, then make the user-doc rule reject student edits to enrolledCourses: `allow update: if isOwner(uid) && !request.resource.data.diff(resource.data).affectedKeys().hasAny(['enrolledCourses','role'])`. Alternatively store enrollment authoritatively in the enrollments collection (server-written) and have isEnrolledStudent read that instead of the self-writable map.",
        "dimension": "firestore-rules-authz",
        "confirmCount": 3,
        "verifierCount": 3
      },
      {
        "title": "Embed scores are client-authored and never re-verified server-side — a student can write a perfect grade directly to their own progress doc",
        "severity": "high",
        "category": "grade-security / broken access control",
        "file": "/Users/lukemccarthy/pantherlearn/src/pages/LessonViewer.jsx (handleAnswer, lines 165-210); /Users/lukemccarthy/pantherlearn/firestore.rules (line 289-292); /Users/lukemccarthy/pantherlearn/scripts/classroom-sync.cjs (line 365-370)",
        "line": "firestore.rules:289-292; LessonViewer.jsx:179-191; classroom-sync.cjs:370",
        "description": "The embed scoring pipeline is end-to-end client-trusted. (1) EmbedBlock.jsx receives an `activityScore` postMessage and calls `onAnswer` (line 112). (2) `onAnswer` is `handleAnswer` in LessonViewer.jsx, which writes the score to `progress/{uid}/courses/{courseId}/lessons/{lessonId}` under `answers.{blockId}` via `updateDoc`/`setDoc` (lines 179-191). (3) firestore.rules line 289-292 grants `allow write: if isOwner(uid)` on the ENTIRE `progress/{uid}/{document=**}` subtree — the student owns their own grade document. (4) classroom-sync.cjs line 370 sums embed grades straight from the stored `a.writtenScore` (`earned += a.writtenScore * pts`) with NO re-grade — unlike MC questions, which are re-checked against `a.correct` at line 296. The autograde Cloud Function (functions/index.js:1571) only fills in short-answer `writtenScore` when it is null (line 1604); it never recomputes or caps embed scores. Net result: the score a student's browser produces becomes their real, Classroom-synced grade with no server validation anywhere in the chain. The postMessage origin check in EmbedBlock is therefore not a real security boundary — it only stops a third-party site, not the authenticated student whose own browser and own Firestore doc are involved.",
        "evidence": "firestore.rules:289  match /progress/{uid}/{document=**} {\n  allow read: if isOwner(uid) || isTeacher();\n  allow write: if isOwner(uid) || isTeacher();\n}\n\nclassroom-sync.cjs:365  embeds.forEach((q) => {\n    const pts = q.weight != null ? q.weight : ...;\n    const a = answers[q.id];\n    ...\n    if (a.writtenScore != null) earned += a.writtenScore * pts;   // trusts client score, no re-grade",
        "exploit_scenario": "A logged-in student opens the lesson page (same origin as the PantherLearn React app), opens devtools, imports the already-loaded Firebase SDK, and runs `updateDoc(doc(db,'progress',myUid,'courses',cid,'lessons',lid),{['answers.'+embedBlockId]:{score:5,maxScore:5,writtenScore:1,submitted:true,completedAt:new Date().toISOString()}})`. The rules permit it (they own the doc). MyGrades shows 100%, the 4 PM classroom-sync pushes the perfect score to Google Classroom. No work was done. The same works for every scored embed in every lesson. (Even simpler: in the iframe console they can `window.parent.postMessage({type:'activityScore',score:5,maxScore:5,gameComplete:true},'*')` — origin/source checks pass because it genuinely is their own iframe.)",
        "proposed_fix": "Embed grades cannot be trusted from the client. Add a server-side authority: (a) write raw embed interaction events to an append-only, student-write-once path and have a Cloud Function (using the canonical answer key / tool logic) compute `writtenScore`, then write the score to a teacher-only-writable field that the student cannot modify; OR (b) at minimum, tighten firestore.rules so the student can create but not arbitrarily overwrite an already-graded `writtenScore` (e.g. reject writes that raise writtenScore without a matching server-verifiable token), and have classroom-sync/autograde recompute embed scores from stored interaction data rather than the self-reported number. The current `allow write: if isOwner(uid)` on the whole progress subtree is the root of the hole. Document this as a known grade-integrity gap regardless of fix path.",
        "dimension": "postmessage-embed-security",
        "confirmCount": 3,
        "verifierCount": 3
      },
      {
        "title": "papsAiBuildsProxy IP rate-limit is trivially bypassed via spoofed X-Forwarded-For header",
        "severity": "high",
        "category": "cloud-functions-authz",
        "file": "/Users/lukemccarthy/pantherlearn/functions/index.js",
        "line": "2882-2933",
        "description": "papsAiBuildsProxy is unauthenticated by design (serves the public paps-ai-builds.web.app student tools). Its only abuse control besides origin-lock + model-allowlist is a per-IP rate limit of 30 req/60s. But the IP is taken from `req.headers['x-forwarded-for']` first, with no validation. A client fully controls that header, so an attacker rotates a fake X-Forwarded-For per request and every request lands in a unique rateLimits doc — the cap never triggers. The origin check (req.headers.origin) is also client-settable for non-browser requests, so a scripted attacker can set Origin: https://paps-ai-builds.web.app and bypass the CORS/origin gate entirely. Net result: the only real protection is the model allowlist, leaving the paid Gemini key open to quota-drain by anyone willing to spoof two headers. It also writes one rateLimits doc per distinct (spoofed) IP, which can balloon Firestore.",
        "evidence": "const origin = req.headers.origin || \"\";\nif (!PAPS_AI_BUILDS_ORIGINS.includes(origin)) { return res.status(403)... }\n...\nconst ip = (req.headers[\"x-forwarded-for\"] || req.ip || \"unknown\").toString().split(\",\")[0].trim();\nconst ipKey = `papsAiBuilds_${ip.replace(/[^a-zA-Z0-9]/g, \"_\")}`;\n// attacker sets X-Forwarded-For: <random> each request -> unique ipKey -> cap never hit",
        "exploit_scenario": "Attacker scripts: for i in 1..100000: POST papsAiBuildsProxy with header `Origin: https://paps-ai-builds.web.app` and `X-Forwarded-For: 10.0.0.$i`, body {model:'gemini-2.5-pro', payload:{...long prompt...}}. Origin check passes (header is forged), each unique XFF defeats the 30/min cap, and gemini-2.5-pro (the most expensive allowlisted model) runs unbounded on Luke's key. Also creates 100k junk rateLimits docs.",
        "proposed_fix": "On Cloud Functions v2 (Cloud Run), trust only the platform-supplied client IP: use the LAST entry of X-Forwarded-For (the load balancer appends the real client IP at the end) or req.ip, not the first/spoofable entry. Better: add a lightweight server-issued token or App Check to gate the proxy, and cap payload size + output tokens. Keep the model allowlist. Treat the origin header as a hint, not a control.",
        "dimension": "cloud-functions-authz",
        "confirmCount": 3,
        "verifierCount": 3
      },
      {
        "title": "All teacher-only routes and pages are gated only in client UI; route protection checks login, not role",
        "severity": "high",
        "category": "auth-role-gating",
        "file": "/Users/lukemccarthy/pantherlearn/src/App.jsx",
        "line": "89-101, 133-168",
        "description": "ProtectedLayout only checks `if (!user) return <Navigate to=\"/login\"/>` — it does NOT check role. Teacher routes /editor, /grading, /rosters, /progress/*, /xp-controls, /mana/:courseId, /teacher/mana-requests, /teacher/show-me, /chatbot-dashboard, /bias-detective-dashboard, /embedding-explorer-dashboard all sit under ProtectedLayout with no role guard at the route layer. Each page enforces role internally (e.g. StudentProgress.jsx:268 `if (userRole !== \"teacher\") return Teacher access only`, GradingDashboard.jsx:509, ShowMeGrader.jsx:197, ManaManager.jsx:223 navigate('/')). This is defense-in-UI only: it relies on userRole, which is itself derived purely client-side from the email pattern in useAuth.getRoleFromEmail (useAuth.jsx:26-45). The data-layer backing (firestore.rules) is the only real boundary — and per the two critical findings above, that boundary is currently bypassable.",
        "evidence": "App.jsx:89-93  function ProtectedLayout(){ const {user,loading}=useAuth(); if(loading) return <LoadingSpinner/>; if(!user) return <Navigate to=\"/login\"/>; ... }\nApp.jsx:138 <Route path=\"/grading\" element={<GradingDashboard/>} />  (no role wrapper)\nuseAuth.jsx:44  return digitCount >= 3 ? \"student\" : \"teacher\";",
        "exploit_scenario": "On its own, a student navigating to /grading just sees the 'Teacher access only' message because userRole is student. But the UI gate is meaningless once role is spoofable (finding 1) or once a student calls the underlying Firestore writes directly (finding 2) — there is no second, server-side line of defense at the route/action layer. The client gates create a false sense of security.",
        "proposed_fix": "Keep the UI gates but stop treating them as the security boundary. Add a role-aware route wrapper (e.g. <TeacherRoute>) that redirects non-teachers, AND ensure every teacher action is independently enforced by firestore.rules using a server-trusted role (custom claim). Audit each teacher write to confirm the rule path requires isCourseTeacher/isTeacher with a trustworthy role source.",
        "dimension": "auth-role-gating",
        "confirmCount": 2,
        "verifierCount": 3
      },
      {
        "title": "Graded block types (ranking, linked, sorting, concept_builder) counted by classroom-sync but completely dropped by MyGrades and StudentProgress — student-facing grades diverge from Google Classroom",
        "severity": "high",
        "category": "grade-loss-correctness",
        "file": "src/pages/MyGrades.jsx (getLessonGrade ~L198-282), src/pages/StudentProgress.jsx (getStudentLessonGrade ~L371-518), scripts/classroom-sync.cjs L242-371",
        "line": "MyGrades.jsx:198-282; StudentProgress.jsx:371-518; classroom-sync.cjs:244-371",
        "description": "classroom-sync.cjs grades SIX question/block families: MC, SA, ranking (partialScore, L307-312), linked (writtenScore, L315-320), sorting (writtenScore or score.correct/total, L323-329), concept_builder (completion, L332-337), teacher_checkpoint, scored embeds, and scored dropdown data_tables. But the two student-facing grade calculators only count a SUBSET. MyGrades.getLessonGrade counts ONLY mc + sa + embeds + teacher_checkpoint + dropdown data_table — it has no case for ranking, linked, sorting, or concept_builder. StudentProgress.getStudentLessonGrade is the same: getMCQuestions/getSAQuestions filter to questionType multiple_choice / short_answer, and there is no branch for ranking/linked/sorting/concept_builder. These block types are real and widespread: `questionType: 'ranking'` appears in seed-energy-skate-park-lab.js, seed-elastic-potential-energy.js, seed-intro-to-energy.js, seed-energy-transformations.js, seed-conservation-bar-charts.js; `questionType: 'linked'` appears in 10+ physics seeds (waves, GPE, electric fields, electric potential, Coulomb's law, current-voltage-resistance, etc.); `type: 'sorting'` appears in 10+ seeds (intro-to-energy, energy-transformations, series-parallel-circuits, course reflections, etc.). QuestionBlock.jsx writes partialScore for ranking (L279) and writtenScore for linked, and SortingBlock.jsx writes writtenScore/score (L143-146) — so the data is being captured and the teacher's Classroom push uses it, but the student never sees those points reflected in MyGrades and the teacher's own StudentProgress gradebook view computes a DIFFERENT grade than what got synced to Classroom.",
        "evidence": "classroom-sync.cjs L244-247: const getRanking=...questionType==='ranking'; getLinked=...'linked'; getSorting=...type==='sorting'; getConceptBuilder=...'concept_builder'. L307-337 add their points to earned/possible. MyGrades.jsx L184-191 only defines getMCQuestions/getSAQuestions/getEmbedBlocks (+ inline checkpoints/dataTables) — grep for 'ranking|linked|sorting|concept_builder' in MyGrades.jsx grade calc returns nothing. StudentProgress.jsx L278-279 getMCQuestions/getSAQuestions; grep for those types in StudentProgress.jsx returns nothing.",
        "exploit_scenario": "A physics lesson (e.g. Energy Skate Park Lab) has 2 MC + 1 ranking + 1 linked question. Student answers all 4: 2/2 MC correct, ranking partialScore 0.5, linked writtenScore 1.0. classroom-sync computes earned=3.5/possible=4 = 88% and pushes 88 to Google Classroom. MyGrades and the teacher's StudentProgress view compute earned=2/possible=2 (only the MC) = 100%. The student sees 100% in the app, Classroom shows 88%, and the teacher's gradebook drilldown shows 100% — three different grades for the same work. If the ranking/linked answers were the student's strong work, the OPPOSITE can happen and the app over-reports; either way the gradebook of record (Classroom) silently disagrees with what students and the teacher see in PantherLearn.",
        "proposed_fix": "Add ranking/linked/sorting/concept_builder handling to BOTH getLessonGrade (MyGrades.jsx) and getStudentLessonGrade (StudentProgress.jsx), mirroring classroom-sync exactly: ranking uses a.partialScore (max 1, count only if submitted); linked uses a.writtenScore (max 1, count if submitted); sorting uses a.writtenScore else a.score.correct/a.score.total (max 1); concept_builder = 1 point if submitted. Better: extract a single shared computeLessonGrade() module imported by all three surfaces so the allowlist can never drift again. Add a unit test asserting identical earned/possible for a fixture lesson across all three.",
        "dimension": "grade-loss-correctness",
        "confirmCount": 3,
        "verifierCount": 3
      },
      {
        "title": "EmbedBlock monotonic writtenScore guard can permanently strand a lesson in 'Not Submitted' — student's grade never registers despite finishing",
        "severity": "high",
        "category": "grade-loss-correctness",
        "file": "src/components/blocks/EmbedBlock.jsx",
        "line": "107-116",
        "description": "The handler returns early when a new score is lower than the stored one: `if (existing?.writtenScore != null && newWrittenScore < existing.writtenScore) return;` (L107). This return happens BEFORE the onAnswer call that sets `submitted`. So the `submitted: isGameComplete || wasAlreadySubmitted` flag (L116) is only ever written when the new score is >= the existing score. If a tool sends an interim high score with gameComplete:false (saving progress), and the student's FINAL run scores lower and sends gameComplete:true, the final message is dropped entirely — the lesson never flips submitted:true. MyGrades requires submitted (getLessonGrade only counts embeds where a.writtenScore != null AND the embed status/Complete-Lesson gating keys off data.submitted), and the scored-embed-checklist says gameComplete:true is what flips submitted:true to unlock Complete Lesson and show the grade. Result: the student finished, sees a lower score correctly stored, but the activity is stuck 'Not Submitted', the Complete Lesson button never unlocks, and classroom-sync (which requires a.submitted at L368) never grades the embed.",
        "evidence": "EmbedBlock.jsx L106-116: `const existing = studentData?.[block.id]; if (existing?.writtenScore != null && newWrittenScore < existing.writtenScore) return; const isGameComplete = msg.gameComplete !== false; const wasAlreadySubmitted = existing?.submitted === true; onAnswer(block.id, { ... submitted: isGameComplete || wasAlreadySubmitted, ... })`. The early return at L107 skips the onAnswer entirely. classroom-sync.cjs L368 `if (!a?.submitted) return;` confirms an embed with no submitted flag is dropped from grading.",
        "exploit_scenario": "Multi-round game tool posts {score:5,maxScore:5,gameComplete:false} after round 1 (writtenScore=1.0 stored), then the student finishes all rounds with a final {score:3,maxScore:5,gameComplete:true}. Because 0.6 < 1.0, the final message is dropped — submitted is never set true. Student sees the 100% interim bar but 'Not Submitted', cannot complete the lesson, and the gradebook gets nothing. Conversely a student who legitimately did worse on the final run is locked out. This is silent grade loss triggered by normal interim-save behavior the scored-embed-checklist explicitly recommends.",
        "proposed_fix": "Separate the score-monotonicity decision from the submitted-flag decision. Always honor gameComplete:true to set submitted, even when the new score is lower. E.g.: compute keepScore = !(existing?.writtenScore != null && newWrittenScore < existing.writtenScore); then call onAnswer with score/writtenScore = keepScore ? new : existing, but ALWAYS set submitted: isGameComplete || wasAlreadySubmitted and update completedAt. Never early-return past the submitted write when gameComplete is true.",
        "dimension": "grade-loss-correctness",
        "confirmCount": 2,
        "verifierCount": 3
      },
      {
        "title": "safeLessonWrite remaps block IDs by fuzzy content similarity — a 60% word-overlap match silently rebinds one student answer onto a different question's block ID",
        "severity": "high",
        "category": "grade-loss-correctness",
        "file": "scripts/safe-lesson-write.cjs",
        "line": "87-135, 100-104",
        "description": "When a lesson with student progress is re-seeded, safeLessonWrite tries to preserve block IDs by matching new blocks to old blocks via content similarity. findBestContentMatch (L87) accepts any match with textSimilarity >= 0.6 (L104), where textSimilarity (L127-135) is a crude shared-word/union-word ratio over lowercased prompt text. Two distinct MC questions in the same lesson that share boilerplate stems (e.g. 'Which of the following best describes...') can exceed 0.6 overlap. When that happens, the NEW block (different correct answer / options) inherits the OLD block's ID, and every student's stored answer for that ID now grades against a different question. Because the ID still matches, neither classroom-sync's orphan-skip guard (L283-291, which only fires when ZERO answer IDs match) nor MyGrades will detect anything wrong — the grade is silently computed against the wrong question. This is exactly the class of silent miscalculation grade-data-integrity warns about, hidden inside the 'safe' writer.",
        "evidence": "safe-lesson-write.cjs L100-104: `if (newText === oldText) return oldBlock; const score = textSimilarity(newText, oldText); if (score > bestScore && score >= 0.6) { bestScore = score; bestMatch = oldBlock; }`. L127-135 textSimilarity = shared/union words. L68-73 positional fallback assigns 'the next unused old ID for this type' even with zero textual match, which can rebind an answer to an unrelated same-type block. classroom-sync.cjs orphan guard L287 only triggers `if (matched === 0)` — a single coincidental match defeats it.",
        "exploit_scenario": "A physics lesson is edited to fix a typo and add one MC question, then re-seeded. Two MC stems share ~65% of their words. safeLessonWrite assigns the new Q4's content the old Q3's block ID (and vice-versa via positional fallback). Students who answered old Q3 correctly now have that answer graded against new Q3's (different) correct option — flipping correct to incorrect for an unknown number of students, with no error, no warning, and an ID that still 'matches' so every downstream guard passes.",
        "proposed_fix": "Do not rely on fuzzy similarity to preserve grade-bearing IDs. Preferred: require seed scripts to carry stable explicit block IDs and have safeLessonWrite preserve only EXACT-ID or EXACT-TEXT matches; for anything else, ABORT the write and report the ambiguous blocks rather than guessing. At minimum: remove the positional fallback (L68-73) for question blocks, raise the similarity threshold to ~0.95, and refuse to remap when two old blocks of the same type are within the threshold of one new block (ambiguous). Log every remap with old/new prompt text for teacher review.",
        "dimension": "grade-loss-correctness",
        "confirmCount": 2,
        "verifierCount": 3
      },
      {
        "title": "ConnectFour: game-completion count double-increments (inflated grade) when a game ends via the AI move or the answer-resolution path",
        "severity": "high",
        "category": "data-corruption / grade-correctness",
        "file": "/Users/lukemccarthy/pantherlearn/src/components/blocks/ConnectFourBlock.jsx",
        "line": "294-298 (handleGameEnd), 242 (AI path), 302-309 (subscription effect), 488-491 (answer path)",
        "description": "handleGameEnd() unconditionally does `setGamesCompleted(gamesCompleted + 1)` with NO per-game guard of its own. It is invoked from three places: (a) the real-time subscription effect at lines 302-309, which IS guarded by `lastCountedGameRef`; (b) the AI-move effect at line 242 (`if (result?.gameOver) handleGameEnd(result)`) which does NOT set/check `lastCountedGameRef`; and (c) the student-answer path at lines 488-491 which DOES check+set the ref. When a game ends on the AI's move, line 242 calls handleGameEnd (increments to N+1) without touching the ref, then the Firestore subscription pushes status:'finished', the effect at 302 sees `lastCountedGameRef.current !== activeGameId` (still null/old), sets the ref, and calls handleGameEnd AGAIN (increments to N+2). One finished AI game is counted twice. getScore() maps gamesCompleted >=3 -> 5/5, ==2 -> 4.25/5, etc., so the inflated count is sent to onAnswer() as the student's grade. A student can reach '3 games / full credit' after finishing only ~2 AI games where the AI made the winning/final move.",
        "evidence": "const handleGameEnd = useCallback((result) => {\n  const newCompleted = gamesCompleted + 1;\n  setGamesCompleted(newCompleted);\n  sendScore(newCompleted, gamesStarted);\n}, [gamesCompleted, gamesStarted, sendScore]);\n...\n// AI move effect (line 242) — NO ref guard:\nif (result?.gameOver) { handleGameEnd(result); }\n...\n// subscription effect (line 302-309) — separate guard:\nif (lastCountedGameRef.current === activeGameId) return;\nlastCountedGameRef.current = activeGameId;\nhandleGameEnd(gameData);",
        "exploit_scenario": "Student plays vs AI. The AI lands the final/winning move. Line 242 fires handleGameEnd (count=1). The subscription then delivers status:'finished'; ref is still null so the effect fires handleGameEnd again (count=2). After a second such game count=4 -> getScore returns 5/5. Student gets full credit for ~2 games instead of the required 3 — a grade-inflation bug keyed to this scored embed block.",
        "proposed_fix": "Move the once-per-game guard INTO handleGameEnd so every entry point is deduped: `const handleGameEnd = useCallback((result) => { if (lastCountedGameRef.current === activeGameId) return; lastCountedGameRef.current = activeGameId; const newCompleted = gamesCompleted + 1; setGamesCompleted(newCompleted); sendScore(newCompleted, gamesStarted); }, [activeGameId, gamesCompleted, gamesStarted, sendScore]);` Then remove the redundant ref checks at the call sites. Also reset lastCountedGameRef in handleBackToLobby so re-opening a finished game can't re-count.",
        "dimension": "general-correctness-bugs",
        "confirmCount": 3,
        "verifierCount": 3
      },
      {
        "title": "Any authenticated user can read ALL students' practice/test sessions (no per-uid scoping)",
        "severity": "high",
        "category": "authorization / data exposure",
        "file": "/Users/lukemccarthy/pantherprep/firestore.rules",
        "line": "21-24",
        "description": "The `sessions` collection rule grants `allow read: if isPapsUser();` — i.e. ANY authenticated paps.net/agent user, including every ordinary student, can read every session document. Session docs are keyed with a `uid` field and contain a student's test/practice session data (see lib/session-history.ts l.27-28,49-50 which query `where('uid','==',uid)` and lib/firestore.ts which writes them). The client always filters by the signed-in uid, but Firestore rules are the real boundary; a student can issue an unfiltered `collection('sessions').get()` from devtools and read everyone's results. This is independent of the role-escalation bug (finding #1) — no escalation needed.",
        "evidence": "match /sessions/{sessionId} {\n  allow read: if isPapsUser();   // every authenticated student can read every session\n  allow create: if isPapsUser();\n}",
        "exploit_scenario": "A signed-in student runs `firebase.firestore().collection('sessions').get()` in the console and receives all students' session documents (scores, answers, timestamps) — a privacy breach with no special privileges required.",
        "proposed_fix": "Scope reads to owner-or-staff like the adaptive collections do: `allow read: if isPapsUser() && (resource.data.uid == request.auth.uid || isStaff(request.auth.uid));`. Also tighten create so a student can only create sessions stamped with their own uid: `allow create: if request.auth != null && request.resource.data.uid == request.auth.uid;`.",
        "dimension": "pantherprep-audit",
        "confirmCount": 3,
        "verifierCount": 3
      },
      {
        "title": "OpenSciEd Physics Units 2-6 are LIVE in Firestore but exist only on an un-merged, un-pushed local branch",
        "severity": "high",
        "category": "work-at-risk",
        "file": "/Users/lukemccarthy/pantherlearn-wt/openscied-u1-grid-sim/scripts/seed-phys-u2-l01-day-earth-cracked.cjs (+ 69 sibling seed-phys-u[2-6]-l*.cjs)",
        "line": "branch openscied-u1-grid-sim @ 5cdd656; commit d5f666c 'feat(phys-u6): 14 lessons seeded — full U2-U6 skeleton complete'",
        "description": "Luke's belief is confirmed and worse than he thought. The course physics-2026 EXISTS LIVE in Firestore with 84 lessons (u1:14, u2:14, u3:14, u4:14, u5:14, u6:14 — verified via firebase-admin query against the production DB), title 'Physics (2026–27)'. The seed scripts that created this live data (70 U2-6 lesson scripts + 14 U1 = full build) are committed ONLY on the local branch openscied-u1-grid-sim (worktree /Users/lukemccarthy/pantherlearn-wt/openscied-u1-grid-sim). That branch is 35 commits ahead of main, is NOT merged into main, and is NOT pushed to origin (git branch -r shows no openscied remote). So the entire next-year physics course (all 6 OpenSciEd units, ~40+ embeds, 70 lesson images) lives in exactly ONE place: this MacBook's local git. A disk failure, an accidental `git worktree remove`, or a `git branch -D` loses the only recoverable source for content that is already serving from production Firestore. GOOD NEWS for grade safety: the seeds route through k.seedLesson -> safeLessonWrite() (unit1-seed-kit.cjs line 119), so they are append-only and did not regenerate IDs; and the live course has 0 student responses (next-year course, not yet in use), so no grades are at risk. The risk is pure work-loss of a very large, completed, already-deployed build.",
        "evidence": "git log main..openscied-u1-grid-sim = 35 commits incl 'feat(phys-u6): 14 lessons seeded — full U2-U6 skeleton complete'. `git branch --merged main | grep openscied` => NOT merged. `git branch -r | grep openscied` => empty (LOCAL ONLY). Firestore admin query: physics-2026 course exists:true, lessons:84, byUnit {u1:14,u2:14,u3:14,u4:14,u5:14,u6:14}, responses:0. Seed write path: seed-phys-u2-l01...cjs line 116 `k.seedLesson(db, COURSE_ID, lesson)` -> unit1-seed-kit.cjs:119 `safeLessonWrite(db, courseId, lesson.id, out)`.",
        "exploit_scenario": "Not an attacker scenario — an operational one. Another parallel Claude/Kimi session (Luke runs 4) prunes stale worktrees, runs `git worktree remove pantherlearn-wt/openscied-u1-grid-sim` and `git branch -D openscied-u1-grid-sim` to 'clean up'. The 35 commits are unreachable and garbage-collected. The physics-2026 course keeps serving from Firestore, but there is now no source to regenerate, fix, image, or re-seed it — and no record on main/origin that the build ever happened. Six units of next-year curriculum become un-maintainable live data.",
        "proposed_fix": "Immediately push the branch to origin: `git -C /Users/lukemccarthy/pantherlearn push -u origin openscied-u1-grid-sim`. Then open a PR / merge to main (review-only here — recommend Luke does the merge since it touches grade-adjacent scripts). Do NOT re-run any seed against physics-2026 as part of merge — it's already live; merging is purely to get the source onto main+origin. Add a note in context/current-priorities.md that physics-2026 is live-from-an-unmerged-branch until the merge lands.",
        "dimension": "uncommitted-work-forensics",
        "confirmCount": 3,
        "verifierCount": 3
      },
      {
        "title": "Uncommitted html-activity-state persistence fix in EmbedBlock.jsx — the #1 data-loss rule fix lives only in the working tree",
        "severity": "high",
        "category": "work-at-risk",
        "file": "/Users/lukemccarthy/pantherlearn/src/components/blocks/EmbedBlock.jsx",
        "line": "uncommitted diff: +61 lines incl handler for msg.type === 'html-activity-state' -> setDoc to responses/{uid}/blocks/{blockId}",
        "description": "EmbedBlock.jsx has 61 uncommitted lines that implement the server-side persistence of student in-tool work (html-activity-state -> Firestore). This is the exact fix mandated by .claude/rules/persist-student-interaction-data.md after the 2026-05-20 incident where 13 students' Electrostatics Mastery Check answers were silently dropped. The handler validates origin (isAllowedOrigin at line 78), persists work state on html-activity-state (line 82), and handles requestAuthToken/activityScore. This grade-and-work-critical fix is currently ONLY in the working tree — one `git checkout -- src/components/blocks/EmbedBlock.jsx` or `git stash` mishap reverts it, silently re-opening the data-loss bug that this whole rule exists to prevent. It is mixed in with 113 other uncommitted files, so it could easily be lost in a bulk discard.",
        "evidence": "git diff EmbedBlock.jsx adds: `import { doc, setDoc, serverTimestamp } from \"firebase/firestore\"`; `if (msg.type === \"html-activity-state\")`; `await setDoc(ref, {...})` persisting to responses path; origin guard `if (!isAllowedOrigin(event.origin)) return;` at line 78. The committed (HEAD) EmbedBlock lacks this — it's a working-tree-only change.",
        "exploit_scenario": "A session doing 'clean up uncommitted noise before a deploy' runs `git stash` or discards working-tree changes to get a clean build. EmbedBlock reverts to the pre-fix version that only handles activityScore. The next time a student uses a tool that produces work (typed answers, built diagrams), the work is dropped again exactly as in the 2026-05-20 incident — and because the regression is invisible (completion flag still saves), no one notices until a teacher needs to recover student work that isn't there.",
        "proposed_fix": "Commit EmbedBlock.jsx (and the companion DataTableBlock.jsx/StudentProgress.jsx/MyGrades.jsx grade-UI changes) on its own focused commit ASAP: `git add src/components/blocks/EmbedBlock.jsx && git commit -m 'fix(embed): persist html-activity-state to Firestore (data-loss rule)'`. Verify the round-trip (test student does an activity -> doc appears at responses/{uid}/blocks/{blockId}) per the rule before relying on it. Highest-priority item to get out of the volatile working tree.",
        "dimension": "uncommitted-work-forensics",
        "confirmCount": 3,
        "verifierCount": 3
      },
      {
        "title": "Student can set own mana balance to any value (economy self-credit)",
        "severity": "medium",
        "category": "firestore-rules-authz",
        "file": "/Users/lukemccarthy/pantherlearn/firestore.rules",
        "line": "128-141",
        "description": "studentMana/{uid} allows `allow write: if isCourseTeacher(courseId) || request.auth.uid == uid`. The inline comment asserts crediting goes through the mageAwardMana/donateMana Cloud Functions which enforce caps, but the rule itself lets a student write ANY field of their own balance doc — including `balance` directly. src/lib/mana.jsx (chargeStudentMana/deductStudentMana) confirms the client writes balance via tx.set/setDoc, so the schema is fully client-writable. The per-student transaction ledger (line 138-141) is likewise self-writable, so a student can also forge history entries.",
        "evidence": "match /studentMana/{uid} {\n  allow read: if hasCourseAccess(courseId);\n  allow write: if isCourseTeacher(courseId) || request.auth.uid == uid;\n  match /transactions/{txId} {\n    allow write: if isCourseTeacher(courseId) || request.auth.uid == uid;\n  }\n}",
        "exploit_scenario": "A student runs `setDoc(doc(db,'courses',courseId,'studentMana',myUid), {balance: 999999}, {merge:true})` in devtools, then redeems real-world perks (3D prints, classwork passes, avatar rentals) they never earned. The Cloud-Function caps are irrelevant because the direct write never goes through them.",
        "proposed_fix": "Make balance/lifetimeEarned server-authoritative: route all spend/charge through Cloud Functions (admin SDK) and change the rule to forbid student writes to economic fields — e.g. allow students to write nothing here (write: if isCourseTeacher), or at most non-economic UI prefs via affectedKeys().hasOnly([...]) that excludes balance, lifetimeEarned, level, history. Same treatment for the transactions ledger (server-write only).",
        "dimension": "firestore-rules-authz",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "Any authenticated user can read every user's profile (PII exposure across all students/teachers)",
        "severity": "medium",
        "category": "firestore-rules-authz",
        "file": "/Users/lukemccarthy/pantherlearn/firestore.rules",
        "line": "71-72",
        "description": "users/{uid} has `allow read: if isLoggedIn()`. Any authenticated student can read EVERY user document in the platform — all students across all courses and all teachers — not just classmates. User docs typically hold name, email, role, enrolledCourses, and gamification/profile data. There is no course-scoping or ownership restriction on read.",
        "evidence": "match /users/{uid} {\n  allow read: if isLoggedIn();",
        "exploit_scenario": "A logged-in 9th grader queries the entire users collection from devtools and exfiltrates names + emails of every student in the school's PantherLearn instance, plus which courses each is enrolled in. FERPA-relevant directory/PII leak.",
        "proposed_fix": "Scope reads. Students should only read their own doc (isOwner) and, if needed for rosters/chat, only docs of users sharing a course — enforce via a query-scoped rule or a server-provided roster. Teachers can keep broad read. Minimal fix: `allow read: if isOwner(uid) || isTeacher()` and add narrow per-feature reads (e.g. course chat members) where cross-student name display is actually required.",
        "dimension": "firestore-rules-authz",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "studentProfiles / classSongs readable & writable by ANY authenticated account (student notes leak + tamper)",
        "severity": "medium",
        "category": "firestore-rules-authz",
        "file": "/Users/lukemccarthy/pantherlearn/firestore.rules",
        "line": "33-40",
        "description": "studentProfiles/{id} and classSongs/{id} both use `allow read, write: if request.auth != null`. The comment says this is a Lachlan-internal obscure-URL UI containing student notes for Suno song generation. But PantherLearn students authenticate too — so every enrolled student (not just the internal tool's anon session) can read and overwrite all student notes and class song drafts. \"Obscure URL\" is not an access control; the data is reachable by collection query from any signed-in client.",
        "evidence": "match /studentProfiles/{id} {\n  allow read, write: if request.auth != null;\n}\nmatch /classSongs/{id} {\n  allow read, write: if request.auth != null;\n}",
        "exploit_scenario": "A student lists the studentProfiles collection from devtools and reads teacher-authored notes about classmates (which may include behavioral/personal observations gathered for song generation), or wipes/overwrites them. Both read and write are open to the whole authenticated population.",
        "proposed_fix": "Lock these to teachers only: `allow read, write: if isTeacher()`. If the internal generator runs as anonymous auth without a teacher role, give that worker a dedicated service identity / custom claim and gate on it. Never rely on URL obscurity for authz.",
        "dimension": "firestore-rules-authz",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "EmbedBlock leaks the student's Firebase ID token to any iframe on an allowed origin via requestAuthToken",
        "severity": "medium",
        "category": "token leakage / auth",
        "file": "/Users/lukemccarthy/pantherlearn/src/components/blocks/EmbedBlock.jsx",
        "line": "87-97",
        "description": "On a `requestAuthToken` message from the embedded iframe, EmbedBlock fetches the logged-in user's Firebase ID token via `user.getIdToken()` and posts it back to the iframe with `event.source.postMessage({type:'authToken', token}, event.origin)`. The token is the student's real Firebase Auth credential. It is handed to whatever HTML is loaded in the embed iframe. The origin allowlist (`isAllowedOrigin`) gates this, but the allowlist is broad: any `https://<anything>-paps.web.app`/`firebaseapp.com` (line 21) and any `paps-tools*/pantherlearn*/pantherprep*/brstatus*` host (line 18). Any single embed tool that is XSS-able, or any future tool deployed to a `-paps` Firebase site by anyone with deploy access, can harvest live student ID tokens and use them to read/write that student's data (including their grades, per the finding above) from anywhere until the token expires (~1 hour).",
        "evidence": "if (msg.type === \"requestAuthToken\") {\n  if (user?.getIdToken) {\n    const token = await user.getIdToken();\n    event.source?.postMessage({ type: \"authToken\", token }, event.origin);\n  }\n  return;\n}",
        "exploit_scenario": "An embed tool with a reflected-XSS sink (e.g. one that echoes a URL param into innerHTML) is loaded in the iframe. Attacker crafts a lesson-embed URL or injects script that calls `parent.postMessage({type:'requestAuthToken'},'*')`, receives the student's ID token in the iframe, and exfiltrates it. With that token the attacker can call Firestore as the student and rewrite their grades (or read other owned data). The broad `-paps.web.app` allowlist also means a compromised/rogue sibling Firebase site qualifies as an allowed origin.",
        "proposed_fix": "Don't broadcast the raw ID token. Prefer same-origin tools (token never crosses an iframe boundary) or pass a short-lived, narrowly-scoped custom token / signed request minted by a Cloud Function for the specific blockId, not the full account ID token. Tighten isAllowedOrigin to an explicit allowlist of the exact tool hosts actually used, drop the open `*-paps` wildcard, and audit every embed tool for XSS before granting it token access. At minimum, gate requestAuthToken behind an exact-origin match rather than the regex wildcard.",
        "dimension": "postmessage-embed-security",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "EmbedBlock accepts score messages with no integrity binding to the activity — gameComplete and maxScore are fully attacker-controlled",
        "severity": "medium",
        "category": "input validation / grade-security",
        "file": "/Users/lukemccarthy/pantherlearn/src/components/blocks/EmbedBlock.jsx",
        "line": "99-120",
        "description": "Beyond the systemic client-trust issue, the handler itself does minimal validation of the activityScore payload. `maxScore` defaults to 100 if absent (line 102) and `score`/`maxScore` are taken verbatim from the message; `writtenScore = min(score/maxScore, 1)` is the only normalization. `gameComplete` defaults to true unless explicitly false (line 109: `msg.gameComplete !== false`), so any message that merely includes `type:'activityScore'` and a `score` flips `submitted:true` and locks in the grade. There is a one-directional guard (line 107: won't lower an existing writtenScore) but nothing that ties the score to actual completion of the embed's work or to the block's expected max. The defaults make it strictly easier to forge a complete, perfect submission.",
        "evidence": "if (msg.type !== \"activityScore\") return;\nif (msg.score == null || !onAnswer) return;\nconst maxScore = msg.maxScore || 100;\nif (maxScore <= 0) return;\nconst newWrittenScore = Math.min(msg.score / maxScore, 1);\n...\nconst isGameComplete = msg.gameComplete !== false;   // defaults TRUE\nonAnswer(block.id, { score: msg.score, maxScore, writtenScore: newWrittenScore, submitted: isGameComplete || wasAlreadySubmitted, ... });",
        "exploit_scenario": "From the embed iframe console (origin/source checks pass since it's the genuine iframe): `parent.postMessage({type:'activityScore',score:1,maxScore:1},'*')`. maxScore validates, writtenScore=1, gameComplete defaults true → submitted:true, perfect grade, no work done. The `score:1/maxScore:1` form sidesteps any expectation that maxScore equals the embed's real total.",
        "proposed_fix": "This is mitigated only by fixing the systemic server-side trust issue (finding 1). As defense-in-depth, require `maxScore` to be present and to match the block's configured expected max (reject otherwise), require `gameComplete` to be explicitly true rather than defaulting, and ideally accept only a server-verifiable score artifact rather than a raw number. Treat the client score as a display hint, never as the grade of record.",
        "dimension": "postmessage-embed-security",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "queryRecipeBot is authenticated but has no rate limit, unlike sibling LLM proxies",
        "severity": "medium",
        "category": "cloud-functions-authz",
        "file": "/Users/lukemccarthy/pantherlearn/functions/index.js",
        "line": "1320-1397",
        "description": "queryRecipeBot verifies a Firebase ID token (good) but, unlike geminiChat/botChat/botEmbed which share a 10-req/60s per-uid transactional rate limiter, queryRecipeBot has NO rate limiting and no per-request input length cap. It forwards the caller's `prompt` to gemini-2.5-flash-lite (1024 output tokens) on every authenticated call. Any signed-in student can loop calls to burn Gemini quota at full speed. cors:true also means any origin can invoke it with a valid token.",
        "evidence": "exports.queryRecipeBot = onRequest({ cors: true, secrets: [geminiApiKey], maxInstances: 10, timeoutSeconds: 60 }, async (req, res) => {\n  ... // verifyIdToken present\n  const { prompt, selectedSources, cleaningDecisions } = req.body;\n  if (!prompt || !selectedSources) return res.status(400)...;\n  // no rate-limit transaction, no prompt length check\n  const data = await callGeminiWithRetry(url, {...});",
        "exploit_scenario": "A logged-in student (or someone who phished a student token) scripts a tight loop against queryRecipeBot; with no per-uid throttle they push hundreds of generateContent calls/min, draining quota and degrading class-wide AI features. Lower severity than geminiProxy because it still requires a valid account.",
        "proposed_fix": "Add the same per-uid transactional rate limit used by geminiChat (10/60s on rateLimits/{uid}), and cap prompt length (e.g. 2000 chars) and selectedSources count. Optionally narrow cors to the PantherLearn origins.",
        "dimension": "cloud-functions-authz",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "viewAsUid teacher impersonation is correctly gated, but inherits the spoofable-role weakness",
        "severity": "medium",
        "category": "auth-role-gating / data-exposure",
        "file": "/Users/lukemccarthy/pantherlearn/src/pages/LessonViewer.jsx",
        "line": "28-39, 79-124",
        "description": "The 'View as student' feature reads viewAsUid from the query string and only honors it when isTeacher (LessonViewer.jsx:28 `const viewAsUid = isTeacher ? searchParams.get('viewAsUid') : null`). It then listens to progress/{viewAsUid}/... (line 81-82) and reads users/{viewAsUid} for the name. At the data layer this is properly backed: progress read requires isOwner || isTeacher (firestore.rules:290), so a plain student CANNOT use ?viewAsUid= to read a peer's submissions — the Firestore read is denied for non-teachers regardless of the URL param. So this is NOT a direct horizontal-access hole for students today. However, it is entirely dependent on isTeacher() being trustworthy: combined with finding 1 (self-escalation), an escalated student gains full peer-submission read via viewAsUid across every course. There is also no audit log when a teacher opens a student's full submissions via viewAsUid (block_reset is logged, viewAs is not).",
        "evidence": "LessonViewer.jsx:28  const viewAsUid = isTeacher ? searchParams.get(\"viewAsUid\") : null;\nLessonViewer.jsx:81-82  const targetUid = viewAsUid || user.uid; const progressRef = doc(db,\"progress\",targetUid,\"courses\",courseId,\"lessons\",lessonId);\nfirestore.rules:290  match /progress/{uid}/{document=**} { allow read: if isOwner(uid) || isTeacher(); }",
        "exploit_scenario": "A student who has self-escalated role to teacher (finding 1) appends ?viewAsUid=<peerUid> to any lesson URL and reads that peer's full answers/chat logs read-only. Without escalation, the Firestore read is denied, so the standalone risk is limited — the finding is the dependency on a spoofable role plus the missing access audit trail for impersonation.",
        "proposed_fix": "Primary fix is resolving role spoofing (finding 1) so isTeacher() is trustworthy. Additionally, write an audit-log entry (teacher uid/email, viewed student uid, lesson, timestamp) whenever viewAsUid is used, mirroring the resetLog pattern already in StudentProgress.handleResetBlock, so impersonation of a student's view is traceable.",
        "dimension": "auth-role-gating",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "Teacher role granted to any @lachlan.internal email purely from client email pattern",
        "severity": "medium",
        "category": "auth-role-gating",
        "file": "/Users/lukemccarthy/pantherlearn/src/hooks/useAuth.jsx",
        "line": "33-44",
        "description": "getRoleFromEmail grants teacher to any address ending in @lachlan.internal and teacher to any @paps.net local-part with fewer than 3 digits. This is a client-side derivation only; the durable authority is the users.role field seeded from it. The @lachlan.internal branch means anyone able to create a Firebase Auth user with such an email (depends on which auth providers are enabled — email/password is enabled for QA per project notes) would be seeded as a teacher in users.role on first login, and thereafter pass every isTeacher() rule. The digit-count heuristic is also brittle: a teacher whose paps.net username happens to contain 3+ digits is mis-classified as a student, and short student usernames as teacher (the STUDENT_OVERRIDES env list is the only mitigation).",
        "evidence": "useAuth.jsx:34  if (lower.endsWith(\"@lachlan.internal\")) return \"teacher\";\nuseAuth.jsx:42-44  const digitCount = (localPart.match(/\\d/g)||[]).length; return digitCount >= 3 ? \"student\" : \"teacher\";",
        "exploit_scenario": "If the email/password provider (kept enabled for QA) allows self-signup, an attacker registers e.g. qa@lachlan.internal, logs into PantherLearn, is auto-seeded users.role='teacher' by syncUserProfile, and gains full teacher access. Even absent self-signup, the pattern makes 'teacher' a property of a guessable email shape rather than an explicitly granted privilege.",
        "proposed_fix": "Do not derive privilege from email shape on the client as the source of truth. Assign role via a server (Cloud Function) using an explicit allowlist of teacher UIDs/emails and set it as a custom claim; treat @lachlan.internal as an explicit, narrowly-scoped allowlist entry rather than a wildcard suffix. Confirm whether email/password self-signup is open and disable open registration for the teacher-granting domains.",
        "dimension": "auth-role-gating",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "Embed-weight denominator (nonEmbedPts) diverges across the three grade calculators — same embed lesson yields different grades in app vs Classroom",
        "severity": "medium",
        "category": "grade-loss-correctness",
        "file": "scripts/classroom-sync.cjs L364, src/pages/MyGrades.jsx L213, src/pages/StudentProgress.jsx L407",
        "line": "classroom-sync.cjs:364; MyGrades.jsx:213; StudentProgress.jsx:407",
        "description": "For a scored embed with NO explicit weight, all three surfaces fall back to a dynamic split pts = nonEmbedPts / embeds.length. But nonEmbedPts is computed from a DIFFERENT set of blocks in each: classroom-sync.cjs L364 = mc + sa + ranking + linked + sorting + conceptBuilder + reflection; MyGrades.jsx L213 = mc + sa + reflection only; StudentProgress.jsx L407 = mc + sa + reflection only. So an unweighted embed in a lesson that also has ranking/linked/sorting questions gets a larger weight in the Classroom calc than in the two app calcs. This is a direct consequence of the allowlist mismatch above but is a distinct math bug even for the embed itself: the embed's contribution to the grade differs between app and Classroom. (Per feedback_embed_weight_5, embeds are SUPPOSED to carry weight:5 explicitly, which sidesteps this — but any embed missing an explicit weight in a mixed lesson is mis-weighted.)",
        "evidence": "classroom-sync.cjs:364 `const nonEmbedPts = mc.length + sa.length + ranking.length + linked.length + sorting.length + conceptBuilder.length + (reflectionCountsHere ? 1 : 0);` vs MyGrades.jsx:213 `const nonEmbedPts = mc.length + sa.length + (hasReflection ? 1 : 0);` vs StudentProgress.jsx:407 `const nonEmbedPts = mc.length + sa.length + ((studentCompleted && reflection) ? 1 : 0);`",
        "exploit_scenario": "Lesson has 2 MC + 2 ranking + 1 unweighted scored embed. Classroom: nonEmbedPts=4, embed pts=4 (embed worth 4/8 of grade). App: nonEmbedPts=2, embed pts=2 (embed worth 2/4 of grade, but ranking points aren't counted at all). Student scores 50% on the embed: Classroom embed contributes 2/8, app contributes 1/4 — different denominators, different final percentages. Grades silently disagree.",
        "proposed_fix": "Make nonEmbedPts identical across all three (include ranking/linked/sorting/conceptBuilder everywhere, per finding above) — or, simplest and safest, require an explicit weight on every scored embed (enforce weight:5 at seed time and in a lint check) so the dynamic fallback is never hit. The shared computeLessonGrade() module proposed above eliminates this by construction.",
        "dimension": "grade-loss-correctness",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "safeLessonWrite uses raw .set() on the whole lesson doc even on the 'has progress' path — any block that fails to remap loses its ID and orphans grades",
        "severity": "medium",
        "category": "grade-loss-correctness",
        "file": "scripts/safe-lesson-write.cjs",
        "line": "54-80",
        "description": "On the progress-preserving path, remappedBlocks is built by map() where a block with no content/positional match simply returns the NEW block unchanged (L57 `if (!oldGroup...) return block;` and L75 `return block;`), keeping its freshly-generated ID. The function then does `lessonRef.set(updatedLesson)` (L79) — a full document overwrite. So a new lesson structure that adds/reorders blocks such that one existing graded block fails to match will write a brand-new ID for that block, orphaning every student's answer for it. Unlike the documented incident's full-wipe, this is a partial silent orphan: most IDs preserved, one or a few regenerated, easy to miss. The .set() also means this is NOT append-only as grade-data-integrity.md claims safeLessonWrite is ('append-only: it adds new blocks but never removes or reorders existing ones') — reordering/removal in newLesson.blocks is faithfully written.",
        "evidence": "safe-lesson-write.cjs L54 `const remappedBlocks = newBlocks.map(block => {...})`; L57 and L75 return the new block with its new id when no match found; L78-79 `const updatedLesson = { ...newLesson, blocks: remappedBlocks, ... }; await lessonRef.set(updatedLesson);`. The function blindly trusts newLesson.blocks ordering/membership — there is no merge against oldBlocks for blocks that were dropped from newLesson.",
        "exploit_scenario": "A re-seed of an AI Literacy lesson with student answers drops an old embed block (or renames its type) so no same-type old group exists. That block's students keep answers under the old ID, but the new doc has a different-ID block; the old answers become orphaned (classroom-sync skips only if ALL match, so partial orphans still mis-sum possible points). Grades shift downward silently because the orphaned block's earned points vanish while possible may still be counted via the new block being unsubmitted.",
        "proposed_fix": "For the has-progress path, verify EVERY old block ID that has student answers is present in remappedBlocks before writing; if any graded old ID would be dropped, abort and report. Use updateDoc with an explicit blocks field rather than .set() of the whole doc, and assert remappedBlocks is a superset of old graded IDs (append-only invariant the docs promise). Add a dry-run/diff mode that prints which old IDs are preserved vs dropped and refuses to write on any drop without an explicit --force-orphan flag.",
        "dimension": "grade-loss-correctness",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "Students can forge their own adaptive mastery profile",
        "severity": "medium",
        "category": "data integrity",
        "file": "/Users/lukemccarthy/pantherprep/firestore.rules",
        "line": "72-78",
        "description": "`adaptiveProfile/{uid}` allows `write: if request.auth != null && request.auth.uid == uid;` with no field/shape validation. The adaptive engine treats this profile as the computed mastery state (lib/adaptive/performance-service.ts). A student can directly write arbitrary mastery values for themselves rather than earning them through answered questions, defeating the adaptive practice logic and corrupting teacher dashboards that aggregate these profiles (performance-service.ts l.272 scans up to 200 adaptiveProfile docs). The immutable per-answer `performanceLog` (l.62-69) is correctly locked, so this is the weak link for grade/mastery integrity.",
        "evidence": "match /adaptiveProfile/{uid} {\n  allow read: if isPapsUser() && (request.auth.uid == uid || isStaff(request.auth.uid));\n  allow write: if request.auth != null && request.auth.uid == uid;  // student forges own mastery, any shape\n}",
        "exploit_scenario": "A student writes `{mastery: {all_skills: 1.0}}` (or similar) to their own adaptiveProfile, marking themselves fully mastered. The dashboard Luke uses to assign work now shows false mastery, and the adaptive engine stops surfacing weak-skill practice for that student.",
        "proposed_fix": "Compute and write adaptiveProfile server-side from the immutable performanceLog via a Cloud Function / scheduled job, and set the rule to `allow write: if false;` for clients. If a client-side write must remain, constrain it with `affectedKeys().hasOnly([...])` and value bounds so a student cannot set arbitrary mastery.",
        "dimension": "pantherprep-audit",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "Two git stashes hold a 466-line Functions/index.js rewrite + StudentProgress changes — at risk of silent loss",
        "severity": "medium",
        "category": "work-at-risk",
        "file": "/Users/lukemccarthy/pantherlearn git stash@{0} and stash@{1}",
        "line": "stash@{0}: Functions/index.js +466/-... , StudentProgress.jsx +92, GradingDashboard.jsx, firebase.json, .firebaserc; stash@{1}: package.json +1, package-lock +1438",
        "description": "There are two WIP stashes. stash@{0} ('WIP on main: f14644b Merge branch claude/adoring-chebyshev') contains a large Functions/index.js change (466 insertions) plus StudentProgress.jsx (+92), GradingDashboard.jsx, firebase.json and .firebaserc edits. Cloud Functions are where classroom-sync / grade-sync logic lives, so a 466-line uncommitted Functions change is potentially grade-pipeline work sitting in a stash. Stashes are the easiest git artifact to lose: they're not branches, not on origin, invisible to `git status`, and a `git stash clear` or `git stash drop` (or applying with conflicts then abandoning) destroys them with no recovery. Nobody reviewing `git status` would even know this work exists. stash@{1} is mostly a dependency lockfile bump (lower value).",
        "evidence": "`git stash list`: stash@{0} WIP on main: f14644b; stash@{1} WIP on main: 313dfc8. `git stash show --stat stash@{0}`: Functions/index.js | 466 ++++, src/pages/StudentProgress.jsx | 92 ++, GradingDashboard.jsx | 13, firebase.json | 35, .firebaserc | 17. 7 files, 521 insertions.",
        "exploit_scenario": "A cleanup session runs `git stash clear` to tidy state, or pops the stash into a dirty tree, hits conflicts, and resets. The 466-line Functions/index.js work (possibly grade-sync logic) is gone with no branch, no commit, no remote copy to recover from. If that code was the only copy of a classroom-sync fix or new function, it's unrecoverable.",
        "proposed_fix": "Triage the stashes before doing anything else with git state. `git stash show -p stash@{0} > /tmp/stash0.patch` to snapshot it to a file (safe backup), then decide: if the Functions work is wanted, branch it (`git stash branch wip-functions-recover stash@{0}`) and commit; if obsolete, confirm with Luke before dropping. Do the same for stash@{1}. Never `git stash clear` until both are dispositioned. Review-only here — recommend Luke or a Claude (not Kimi) session handle the Functions/index.js review since it's grade-pipeline code.",
        "dimension": "uncommitted-work-forensics",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "Substantial uncommitted work in worktree bold-herschel (Momentum Mystery Lab + new block components + storage.rules) on a local-only branch",
        "severity": "medium",
        "category": "work-at-risk",
        "file": "/Users/lukemccarthy/pantherlearn/.claude/worktrees/bold-herschel",
        "line": "branch claude/bold-herschel @ fec6c5e; 22 modified/untracked files incl src/momentum-mystery-lab.jsx, src/components/blocks/ConceptBuilderBlock.jsx, MediaUploadBlock.jsx, storage.rules, seed-ability-scavenger-hunt.js",
        "description": "The bold-herschel worktree has 22 uncommitted changes on top of its branch tip, including modifications to grade-critical UI (MyGrades.jsx, StudentProgress.jsx, LessonViewer.jsx, LessonEditor.jsx, GradingDashboard.jsx), the Momentum Mystery Lab activity, two brand-new untracked block components (ConceptBuilderBlock.jsx, MediaUploadBlock.jsx), an untracked storage.rules, and untracked seed scripts (seed-ability-scavenger-hunt.js, seed-prompt-engineering-guess-who.js). The branch claude/bold-herschel is LOCAL ONLY (not on origin). So both the committed branch work AND a layer of uncommitted-on-top work exist nowhere but this disk. New block components are particularly risky — if they're referenced by seeded lessons (per feedback_lessonviewer_block_allowlist), losing the component source means those blocks render empty. An untracked storage.rules is a security-config file that could silently diverge from the deployed rules.",
        "evidence": "`git -C .claude/worktrees/bold-herschel status --short`: M MyGrades.jsx, M StudentProgress.jsx, M LessonViewer.jsx, M LessonEditor.jsx, M GradingDashboard.jsx, M src/momentum-mystery-lab.jsx, ?? src/components/blocks/ConceptBuilderBlock.jsx, ?? src/components/blocks/MediaUploadBlock.jsx, ?? storage.rules, ?? seed-ability-scavenger-hunt.js, ?? seed-prompt-engineering-guess-who.js (22 entries total). Branch not on origin.",
        "exploit_scenario": "`git worktree remove --force .claude/worktrees/bold-herschel` (or pruning all .claude/worktrees during cleanup) discards the 22 uncommitted changes instantly — git does not preserve uncommitted worktree state on removal. The new ConceptBuilderBlock/MediaUploadBlock source and the Momentum Mystery Lab edits are gone. If any seeded lesson references those block types, students see empty blocks with no source to restore.",
        "proposed_fix": "In each worktree with uncommitted work (bold-herschel especially; also distracted-wing has ExternalActivityBlock.jsx untracked + BlockRenderer/LessonEditor mods, beautiful-kilby has a Functions/index.js mod), commit the work to its branch and push: `git -C .claude/worktrees/bold-herschel add -A && git -C .claude/worktrees/bold-herschel commit -m 'wip: momentum lab + new blocks + storage.rules' && git -C .claude/worktrees/bold-herschel push -u origin claude/bold-herschel`. Do NOT remove any worktree until its uncommitted changes are committed+pushed. Review storage.rules diff against deployed rules before merging (security config).",
        "dimension": "uncommitted-work-forensics",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "14 untracked seed scripts + 2 new block components in main working tree — local-only, some grade-touching",
        "severity": "medium",
        "category": "work-at-risk",
        "file": "/Users/lukemccarthy/pantherlearn/scripts/ (14 untracked .cjs/.js) + src/components/blocks/LevelProgressBlock.jsx, VideoOrLinkSubmitBlock.jsx",
        "line": "?? scripts/seed-circuit-build-lab.cjs, seed-crack-the-circuit-challenge.cjs, seed-physics-show-me-surge-protector.cjs, seed-dl-course-reflection.cjs, exempt-choice-project.cjs, give-diorvis-33-mana.cjs, etc.; ?? src/components/blocks/LevelProgressBlock.jsx, VideoOrLinkSubmitBlock.jsx",
        "description": "The main repo working tree has 14 untracked seed scripts and 2 brand-new untracked block components (LevelProgressBlock.jsx, VideoOrLinkSubmitBlock.jsx). Several seeds are grade-relevant: exempt-choice-project.cjs (toggles per-student exemptions on grades — the choice-project bulk-exempt tool referenced in current-priorities), seed-physics-show-me-surge-protector.cjs, seed-crack-the-circuit-challenge.cjs, seed-dl-course-reflection.cjs. These have been (or will be) run against live Firestore, so their source is the audit trail / re-run capability for live content and grade operations. New block components that aren't committed but are referenced by seeded lessons render empty (per the LessonViewer allowlist rule). All local-only, vulnerable to bulk discard or `git clean`.",
        "evidence": "`git status --short | grep '^?? scripts/' | wc -l` = 14. Notable: scripts/exempt-choice-project.cjs (grade exemption tool), seed-physics-show-me-surge-protector.cjs, seed-crack-the-circuit-challenge.cjs, seed-circuit-build-lab.cjs, seed-dl-course-reflection.cjs. Untracked components: src/components/blocks/LevelProgressBlock.jsx, src/components/blocks/VideoOrLinkSubmitBlock.jsx.",
        "exploit_scenario": "`git clean -fd` (common 'reset to clean state' command) deletes all 14 untracked seeds and both new block components. If a grade operation later needs to be reproduced or audited (e.g. which students were exempted by exempt-choice-project.cjs), the script is gone. If LevelProgressBlock/VideoOrLinkSubmitBlock are referenced by any seeded lesson, those lessons render empty blocks for students with no source to restore.",
        "proposed_fix": "Commit the untracked scripts and components (group logically): `git -C /Users/lukemccarthy/pantherlearn add src/components/blocks/LevelProgressBlock.jsx src/components/blocks/VideoOrLinkSubmitBlock.jsx scripts/*.cjs scripts/*.js` (review each first), commit, push. Before relying on `git clean` anywhere in this repo, snapshot untracked files. For the new block components, confirm they have a case in LessonViewer.jsx blocksWithProps allowlist (feedback_lessonviewer_block_allowlist) so they don't render empty.",
        "dimension": "uncommitted-work-forensics",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "pantherTrack class creation/read open to any authenticated user (cross-teacher behavior-data read)",
        "severity": "low",
        "category": "firestore-rules-authz",
        "file": "/Users/lukemccarthy/pantherlearn/firestore.rules",
        "line": "366-415",
        "description": "pantherTrack/classes/list/{classId} grants `allow read: if isLoggedIn()` on the class and ALL its subcollections (students, events, deputies, rewards, redemptions, notes). The notes subcollection is described as teacher-only but its read is just isLoggedIn(). So any authenticated paps user (including students) can read every teacher's behavior-tracking classes, student rosters, event logs, and private behavior notes. Create is also `allow create: if isLoggedIn()` (any user can create a class). Writes are correctly gated on teacherUid, but reads are wide open.",
        "evidence": "match /pantherTrack/classes/list/{classId} {\n  allow read: if isLoggedIn();\n  allow create: if isLoggedIn();\n  ...\n  match /notes/{noteId} {\n    allow read: if isLoggedIn();",
        "exploit_scenario": "A student enumerates pantherTrack/classes/list and reads teacher-authored behavior notes and event histories about themselves and classmates across all teachers using the tool — sensitive disciplinary/behavioral PII never meant for students.",
        "proposed_fix": "Restrict reads to the owning teacher (and any explicitly listed co-teachers): `allow read: if isLoggedIn() && resource.data.teacherUid == request.auth.uid` on the class doc, and gate each subcollection read on the parent class teacherUid (as the writes already do). The notes subcollection especially must be teacher-only on read.",
        "dimension": "firestore-rules-authz",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "pantherprep adaptiveProfile/studyPlans student-writable mastery state (self-reported progress, low grade impact)",
        "severity": "low",
        "category": "firestore-rules-authz",
        "file": "/Users/lukemccarthy/pantherprep/firestore.rules",
        "line": "72-77, 138-153",
        "description": "In pantherprep, adaptiveProfile/{uid} allows the student to write their own computed mastery doc (`allow write: if request.auth.uid == uid`), and studyPlans updates are self-writable (with an affectedKeys allowlist). This lets a student fabricate their own mastery/skill state. PantherPrep is a free after-school prep tool, not a graded course, so impact is limited to skewed adaptive recommendations and dashboard accuracy — not a real grade. performanceLog answers are correctly immutable (update,delete: if false), which is the important integrity control. Flagging so it's a conscious choice rather than an oversight.",
        "evidence": "match /adaptiveProfile/{uid} {\n  allow read: if isPapsUser() && (request.auth.uid == uid || isStaff(request.auth.uid));\n  allow write: if request.auth != null && request.auth.uid == uid;\n}",
        "exploit_scenario": "A student overwrites adaptiveProfile to mark all skills mastered, gaming the teacher dashboard / coach-chat triggers into thinking they're proficient. No grade is affected; it degrades coaching accuracy.",
        "proposed_fix": "If teacher-facing dashboards or coach triggers depend on adaptiveProfile/studyPlans being trustworthy, make the mastery fields server-authoritative (written only by the adaptive-engine Cloud Function / admin SDK) and remove student write from those computed fields. Otherwise document that this state is self-reported and untrusted.",
        "dimension": "firestore-rules-authz",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "Hardcoded client-side quiz-unlock bypass code in circuits-quiz tool",
        "severity": "low",
        "category": "client-side-secrets",
        "file": "/Users/lukemccarthy/pantherlearn/public/tools/circuits-quiz/index.html",
        "line": "196, 527",
        "description": "The circuits quiz hardcodes a make-up/QA bypass code MAKEUP_CODE = \"p1unlock\" in shipped client JS. Typing it on the lock screen sets bypassWindow=true and opens the timed quiz (8:20-8:42 AM window) outside its window. This is not an API secret or a grade-spoofing vector (answers are still scored normally), but it is a credential embedded in client code: any student who views source can take the quiz at any time, including make-ups they were not authorized for. Severity is low because the quiz window has already passed (TARGET_DATE 2026-05-21) and the gate is a convenience lock, not a security boundary.",
        "evidence": "index.html:196 var MAKEUP_CODE = \"p1unlock\"; // teacher types this on the lock screen for make-ups / QA  ;  index.html:527 if(code===MAKEUP_CODE){ bypassWindow=true; afterUnlock(); }",
        "exploit_scenario": "A student opens DevTools / View Source on the embedded quiz, reads p1unlock, and uses it to start or restart the timed quiz outside the proctored 8:20-8:42 AM window (e.g., at home before class), defeating the timing control.",
        "proposed_fix": "For future timed-quiz tools, do not ship the unlock code in client JS. Gate make-up access server-side (Cloud Function verifies a teacher-issued one-time token, or check the requester's role/uid against a make-up allowlist in Firestore) so the bypass cannot be discovered by viewing source. No change needed to this specific past-dated quiz.",
        "dimension": "client-side-secrets",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "AI proxy prompts forwarded to Gemini/Anthropic without input-size caps on several functions (quota + cost amplification)",
        "severity": "low",
        "category": "cloud-functions-authz",
        "file": "/Users/lukemccarthy/pantherlearn/functions/index.js",
        "line": "1488,1320,711",
        "description": "Beyond the auth issues, geminiProxy (studentPrompt), queryRecipeBot (prompt), and generateQuestion (blocks array) forward caller-supplied text to the LLM with no maximum input length validation. botChat and botEmbed DO cap inputs (systemPrompt<=2000, messages<=50, phrases<=50) — the pattern exists but isn't applied consistently. Large inputs multiply input-token cost per call and, combined with the missing rate limits above, amplify quota-drain.",
        "evidence": "// botChat (good): if (systemPrompt.length > 2000) ...; if (messages.length > 50) ...\n// geminiProxy (no cap): userPrompt = studentPrompt;  // forwarded as-is\n// queryRecipeBot (no cap): contents: [{ role: 'user', parts: [{ text: prompt }] }]\n// generateQuestion (no cap on blocks): blockSummary built from arbitrary-length blocks",
        "exploit_scenario": "An attacker (anonymous on geminiProxy, authed on queryRecipeBot) sends maximum-size request bodies on every call, maximizing billed input tokens per request and accelerating both cost and quota exhaustion.",
        "proposed_fix": "Apply consistent input caps everywhere prompts reach an LLM: enforce a max length on studentPrompt/prompt (e.g. 2000-4000 chars) and a max blocks/array count, mirroring the botChat/botEmbed validation already in this file.",
        "dimension": "cloud-functions-authz",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "Open read/write rules on secretChat + paps-ai-builds memories (auth-less); acceptable-by-design but worth confirming isolation",
        "severity": "info",
        "category": "firestore-rules-authz",
        "file": "/Users/lukemccarthy/pantherlearn/firestore.rules",
        "line": "7-28",
        "description": "secretChatSignaling, secretChatRooms, and artifacts/paps-ai-builds-jarvis/.../memories all use `allow read, write: if true` (no auth). The comments justify this (ephemeral WebRTC signaling, AES-GCM ciphertext relay, no-PII JARVIS memory keyed by localStorage UUID). This is intentional and isolated to its own collections with no grade/PII data. Flagging for completeness only: anyone on the internet can read/write/enumerate these collections, fill them (storage abuse / spam), and read all secretChat ciphertext + room metadata. Confirm no plaintext PII or recoverable identifiers ever land here, and consider a TTL / size cap to prevent abuse.",
        "evidence": "match /secretChatSignaling/{roomCode} { allow read, write: if true; ... }\nmatch /secretChatRooms/{roomId} { allow read, write: if true; ... }\nmatch /artifacts/paps-ai-builds-jarvis/users/{uid}/memories/{docId} { allow read, write: if true; }",
        "exploit_scenario": "An unauthenticated actor enumerates secretChatRooms, harvests all ciphertext + room codes, and floods the collection with junk docs (no quota/auth gate), inflating Firestore cost and potentially breaking the chat tool.",
        "proposed_fix": "Keep open if truly required for the auth-less student build site, but add defense-in-depth: validate doc shape (size limits, required ciphertext field, max field count), add a TTL field + scheduled cleanup, and confirm via code review that no plaintext identifiers are stored. Prefer anonymous Firebase auth + `if request.auth != null` over fully open if the tool can sign in anonymously.",
        "dimension": "firestore-rules-authz",
        "confirmCount": 1,
        "verifierCount": 1
      },
      {
        "title": "All client-side AI inference correctly routed through Cloud Function proxies — no direct LLM calls from browser",
        "severity": "info",
        "category": "client-side-secrets",
        "file": "/Users/lukemccarthy/pantherlearn/src/lib/api.jsx",
        "line": "api.jsx:5 and related",
        "description": "Every AI/LLM call in the PantherLearn client targets a *.cloudfunctions.net proxy endpoint rather than generativelanguage.googleapis.com / api.openai.com / api.anthropic.com directly. Confirmed endpoints: geminiChat (src/lib/api.jsx:5), botChat + botEmbed (src/pages/BotBuilder.jsx, chatbot-workshop/BotTestView.jsx, IntentClassifierEditor.jsx), generateQuestion (src/lib/aiQuestionGenerator.jsx:7), generateBaselines (src/lib/aiBaselines.jsx:16), validateReflection (src/components/LessonCompleteButton.jsx:12, PhaseReflectionModal.jsx:12), translateText (src/App.jsx:58). Grep for generativelanguage/openai/anthropic/x-goog-api-key/key=AIza across public/tools and src found zero direct provider calls. No model-provider API key is exposed because the key lives server-side in the function. PantherPrep client makes no direct AI calls at all.",
        "evidence": "src/lib/api.jsx:5 const ENDPOINT = 'https://us-central1-pantherlearn-d6f7c.cloudfunctions.net/geminiChat'; ... headers: { Authorization: `Bearer ${authToken}` } where authToken is a Firebase ID token, not a static secret. Same dynamic-token pattern across all listed files; no hardcoded bearer values anywhere.",
        "proposed_fix": "None — this is the correct papsAiBuildsProxy-style architecture. Maintain it for any new tool/embed (never embed a Gemini/OpenAI/Anthropic key in HTML/JS).",
        "dimension": "client-side-secrets",
        "confirmCount": 1,
        "verifierCount": 1
      }
    ],
    "unverified": [],
    "refuted": [
      {
        "title": "biasInvestigations / botReflections / momentumAttempts read uses resource.data ownership but create allows cross-course writes via hasCourseAccess only on some paths",
        "dimension": "firestore-rules-authz",
        "severity": "low",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "REFUTED. The finding is internally inconsistent and its two load-bearing exploit claims are false against the actual code in /Users/lukemccarthy/pantherlearn/firestore.rules.\n\n1) TITLE IS INVERTED. The title names biasInvestigations/botReflections/momentumAttempts as the vulnerable collections. Those are exactly the correctly owner-scoped ones: momentumAttempts (lines 201-206) gates read+write on `request.auth.uid == uid`; biasInvestigations (207-216) gates every op on `resource.data.studentId == request.auth.uid` (and create on `request.resource.data.studentId == request.auth.uid`); botReflections (217-224) gates write on both a docID-prefix match (`reflectionId.matches(request.auth.uid + \"_.*\")`) and `studentId == request.auth.uid`. No cross-course write hole exists in the named collections.\n\n2) BOSSBATTLE PLAYER-PROGRESS CLAIM IS FALSE. src/lib/bossBattle.jsx confirms per-player state (HP, abilities, progress) lives in courses/{c}/bossBattles/{battleId}/players/{uid}, which is owner-scoped in rules (line 166: `allow write: if isOwner(uid) || isCourseTeacher`). A classmate CANNOT update another student's player progress. Only the parent bossBattles/{battleId} doc (shared boss/class state) is writable by participants (line 161) — intended cooperative-activity design.\n\n3) MULTIPLAYER DOCS ARE SHARED BY DESIGN. src/lib/connectFour.js shows connectFourGames (and guessWhoGames) are 2-player docs where challenger AND opponent both write the same document (board/moves/currentTurn). Sole-owner gating would break the games. The finding itself concedes this in its proposed_fix, downgrading the issue to an unconstrained-affectedKeys nitpick, not an authz break.\n\n4) Only perkRequests create=hasCourseAccess (line 232) is cleanly true, but creating a perk request in your own enrolled course is the intended action; update/delete are teacher-only (line 233) and no field-ownership/identity-forgery harm was demonstrated.\n\n5) No grade/data-integrity impact (finding concedes). Scope is intra-class gameplay state within an already-enrolled course. The accurate residue (classmates can tamper with shared cooperative/multiplayer gameplay docs) is by-design shared-write, not a real authorization vulnerability as described.",
            "sev": "info"
          }
        ]
      },
      {
        "title": "No leaked client-side secrets found — both repos clean on this dimension",
        "dimension": "client-side-secrets",
        "severity": "info",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "The finding's factual claims are all accurate, but it is an info-level \"all-clear\" report, not an actual vulnerability — so real=false (there is no security issue to report).\n\nVerified line-by-line:\n- /Users/lukemccarthy/pantherlearn/public/firebase-messaging-sw.js:11 — apiKey \"AIzaSyAlxvGxLIBUrVO3WWmEcslFpSygeYVeHpY\" inside a standard firebaseConfig (authDomain/projectId/storageBucket/messagingSenderId/appId all present, project pantherlearn-d6f7c). This is the Firebase web apiKey, explicitly the one key safe client-side per project standards.\n- /Users/lukemccarthy/pantherprep/lib/firebase-config.ts:2 — apiKey \"AIzaSyD_okYkxb0Faye3kV2GLlVjyMI6CCs8apg\" in a standard firebaseConfig for pantherprep-a5a73. Same — Firebase web key, out of scope.\n- circuits-quiz/index.html:199 self-label \"public Firebase web key (safe client-side)\" confirmed verbatim.\n\nI ran the adversarial grep myself to try to REFUTE the \"clean\" claim by finding a real leaked secret: grepped public/ + src/ (PantherLearn) and app/, components/, lib/, hooks/, public/, out/ (PantherPrep) for sk-, sk-ant-, sk-proj-, AKIA, xoxb-, ghp_, BEGIN PRIVATE KEY, service_account, private_key. Zero hits. A full AIzaSy enumeration returned only the two Firebase web keys named in the finding (correctly scoped to their projects). A targeted apiKey/secret/bearer scan of public/tools/ surfaced no additional inline keys.\n\nThe finding is correct and there is no exploitable secret leak — so this is not a real issue requiring a fix. (Its optional note about adding Google Cloud API-key referrer restrictions is reasonable defense-in-depth but, as the finding itself states, not a leak.)",
            "sev": "info"
          }
        ]
      },
      {
        "title": "generateImage class-period gate trusts uid->enrollment but spoofed timezone/window only blocks off-hours abuse, not in-class quota abuse; teacher bypass keyed on email string",
        "dimension": "cloud-functions-authz",
        "severity": "low",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "The code description is accurate (functions/index.js:2032-2033, 2098-2106 match the evidence: teacher bypass via email.includes(\"@lachlan.internal\"), 10/min + 30/hr per-uid caps). But the finding's exploit premise is refuted on multiple counts:\n\n1) The exploit (\"attacker provisions or obtains any account whose email contains @lachlan.internal\") is not demonstrable. There is NO client-side account creation anywhere in src/ — only signInWithEmailAndPassword (login). Google sign-in cannot mint a non-existent domain like lachlan.internal. The only path to an @lachlan.internal account is the Firebase Admin SDK (setup-qa-student.cjs -> admin.auth().createUser), which requires Luke's admin service-account key. Anyone holding that key already has full game-over access; the period gate is moot for them. The domain is therefore NOT \"loosely provisioned\" as the finding speculates.\n\n2) The title claim of a \"spoofed timezone\" is factually wrong: the timezone/window is computed server-side from the server clock (new Date().toLocaleString(..., {timeZone:\"America/New_York\"}), line 2035), not from any client-supplied value. It cannot be spoofed.\n\n3) Residual real risk is tiny and is a *different* issue than the one described: the QA password (qa-student-2026) is hardcoded in a committed script, and generateImage elevates any @lachlan.internal to teacher (while useAuth treats that same account as a student). Someone reading the repo could log in as QA and skip the per-block image caps — but that's a credential-in-repo concern, bounded by maxInstances:10 on the call, not the authz-design flaw the finding frames.\n\n4) Weakness #2 (full class can collectively make 30/hr each) is by-design in-class behavior with an explicit acceptable cost bound, not an authz gap.\n\nThe substring match technically exists and the proposed custom-claim fix is reasonable hardening, but the concrete exploit is not demonstrable (no signup, non-provisionable domain, server-derived timezone). At most this is an info-level defense-in-depth note, so the finding as stated (low-severity exploitable authz issue) is not real.",
            "sev": "info"
          }
        ]
      },
      {
        "title": "renderMarkdown HTML-escapes student content before markdown/KaTeX, so stored XSS via free-response/chat is currently blocked (defense relies on one helper)",
        "dimension": "xss-stored-injection",
        "severity": "info",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "The finding's code description is fully accurate, but it documents the ABSENCE of a vulnerability — there is no real, exploitable XSS issue. Verified line-by-line in /Users/lukemccarthy/pantherlearn/src/lib/utils.jsx:\n\n1. inlineMarkdown (line 62-72) calls escapeHTML(text) FIRST (line 63) before any HTML-producing regex. Confirmed empirically: `<img src=x onerror=alert(1)>` renders as `&lt;img src=x onerror=alert(1)&gt;` (inert text).\n2. Attribute breakout via crafted markdown-image alt text is blocked — quotes are already entity-encoded (`&quot;`) by the time alt/src are captured. Tested: `![\" onload=alert(1) x=\"](http://x)` produces `alt=\"&quot; onload=alert(1) x=&quot;\"`, no breakout.\n3. sanitizeHref (lines 51-60) rejects javascript:/data:/vbscript: → returns \"#\". Tested all three protocols on both link and image src; all neutralized.\n4. KaTeX renderToString (lines 20,35) uses throwOnError:false with NO trust option. Verified against installed katex@0.16.28: `\\href{javascript:alert(1)}{x}` renders as inert red error text; the \"javascript:\" string appears only inside the non-rendered MathML <annotation>, never as a clickable href. trust defaults to false. (An initial grep-based \"leak\" hit was a false positive matching the annotation text, not a real attribute.)\n5. The sink chain claim is also accurate: student chat content reaches the teacher-facing view through this renderer — ChatLogCard.jsx:86 pipes msg.content (including role===\"user\" student messages) through renderMarkdown into dangerouslySetInnerHTML. So the defense genuinely matters and genuinely holds.\n\nNet: the cited code exists and behaves exactly as described, and the described behavior is SAFE. The finding explicitly sets severity:info and states \"Not currently exploitable.\" There is no demonstrable security hole to fix in the working tree, so as a security finding it is real=false. The proposed regression tests / DOMPurify hardening are reasonable defensive hygiene but are hardening suggestions, not a present vulnerability.",
            "sev": "info"
          }
        ]
      },
      {
        "title": "Teacher GradingDashboard renders student-authored AI-chat messages through renderMarkdown (dangerouslySetInnerHTML) — the highest-value student->teacher sink; safe only via escapeHTML",
        "dimension": "xss-stored-injection",
        "severity": "low",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "Code citations are accurate but the finding describes no present, exploitable vulnerability — it is defense-in-depth hardening dressed as a finding. Verified against actual files:\n\nChatLogCard.jsx:86 does render student-controlled msg.content via dangerouslySetInnerHTML using renderMarkdown(), and ChatbotBlock.jsx:67 does store raw student input as {role:'user', content: input.trim()}. Those facts check out. BUT the mitigating control is real and effective in the current working tree: renderMarkdown (src/lib/utils.jsx:74) routes ALL text through inlineMarkdown (line 62) which calls escapeHTML FIRST (line 63), escaping <, >, &, \", ' (utils.jsx:42-49). The only HTML-emitting markdown (images/links) sanitizes URLs via sanitizeHref (line 51), which rejects javascript:/data:/vbscript:. So <script> and <img onerror> are genuinely neutralized — the finding itself concedes this.\n\nThe finding then relies entirely on two hypotheticals: (1) a FUTURE regression to renderMarkdown (or katex trust:true), and (2) the ': msg.content' raw fallback firing if renderMarkdown were 'undefined/not-a-function at runtime.' Neither is demonstrable. (1) is speculation about code that doesn't exist — by that standard every escaping helper is a latent XSS. (2) is dead code: renderMarkdown is a normal named `export function` statically imported at the top of ChatLogCard; typeof renderMarkdown === 'function' is true at runtime and the fallback branch is unreachable. There is no real import/tree-shake path that yields undefined while still rendering the component.\n\nNo concrete exploit exists against the code as written. This is a low-value hardening note (prefer linkifyText/React children over dangerouslySetInnerHTML), not a stored XSS. Refuted.",
            "sev": "info"
          }
        ]
      },
      {
        "title": "Short-answer grading (WrittenResponseCard) uses the safe linkifyText path — confirm new student-text grading views copy this pattern, not ChatLogCard's",
        "dimension": "xss-stored-injection",
        "severity": "info",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "The finding's code-accuracy claims are all literally correct, but it describes NO vulnerability — so as a security finding requiring action it is not \"real\" (the finding itself says \"No fix needed\").\n\nVerified line-by-line:\n- /Users/lukemccarthy/pantherlearn/src/components/grading/WrittenResponseCard.jsx:284-285 renders the untrusted student field via `{linkifyText(item.answer)}` inside a `whiteSpace:pre-wrap` div — confirmed exactly.\n- item.feedback (line 297) and item.reviewNote (line 410) are rendered as plain JSX text children — React auto-escapes them. Confirmed.\n- linkifyText (src/lib/utils.jsx:125-157) returns an array of React string-parts and React `<a>` elements (never an HTML string), only matches http(s)/www via regex, builds anchors with target=_blank rel=\"noopener noreferrer\", and carries the exact cited comment \"student input is untrusted; we only auto-link http(s) / www URLs.\" There is no dangerouslySetInnerHTML anywhere in this path.\n\nSo the asserted facts are accurate and this surface is XSS-safe by construction. This is an informational confirmation note, not an exploitable issue. There is nothing to remediate in WrittenResponseCard. The only actionable item it gestures at — ChatLogCard's dangerouslySetInnerHTML — is a different file and not part of this finding's concrete claim, so it cannot make this finding \"real.\" Marking real=false: no demonstrable vulnerability in the cited code.",
            "sev": "info"
          }
        ]
      },
      {
        "title": "data_table grading guards differ between MyGrades and StudentProgress/classroom-sync (maxScore presence) — grade mismatch on malformed data_table answers",
        "dimension": "grade-loss-correctness",
        "severity": "low",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "The cited lines exist verbatim, but the finding's grade-mismatch consequence depends on a data state that cannot be produced. The exploit requires a data_table answer with numeric `score` but missing/zero `maxScore`. DataTableBlock.handleSubmit (src/components/blocks/DataTableBlock.jsx L483-501) is the SOLE writer of data_table answers in the entire codebase (confirmed by grep) and always writes `score` and `maxScore: weight` together atomically in one onAnswer object. No legacy seed or alternate component writes the malformed shape; the \"older tool version\" is unsubstantiated speculation.\n\nIn the actual reachable case, maxScore === weight === (b.weight ?? 1) and the score is pre-scaled to weight by DataTableBlock ((correct/totalCells)*weight). All three surfaces therefore compute the identical fraction score/weight: MyGrades (earnedPoints+=score over totalPoints+=weight), StudentProgress (points:score, max:maxScore=weight), classroom-sync (earned+=clamp(score,0,weight) where clamp is a no-op on legit data, possible+=weight). They agree.\n\nThe only genuine difference — unsubmitted/past-due inclusion — is mitigated by gating: StudentProgress excludes the item only before due date (isPastDue branch), and MyGrades's getResult is only invoked for released/past-due lessons (L288 `if (!isDueDatePassed(lesson)) return`). classroom-sync comments (L351-353) explicitly state it mirrors MyGrades. Once grades are released, the surfaces converge. This is a theoretical guard asymmetry with no reachable triggering input — not a demonstrable grade-loss/mismatch bug.",
            "sev": "info"
          }
        ]
      },
      {
        "title": "ConnectFour: handleGameEnd uses stale gamesCompleted from closure when two games finish in quick succession",
        "dimension": "general-correctness-bugs",
        "severity": "medium",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "medium",
            "reasoning": "The cited code exists verbatim. /Users/lukemccarthy/pantherlearn/src/components/blocks/ConnectFourBlock.jsx lines 294-298: `const newCompleted = gamesCompleted + 1; setGamesCompleted(newCompleted); sendScore(newCompleted, gamesStarted);` with deps [gamesCompleted, gamesStarted, sendScore]. The abstract observation (uses closure value instead of functional updater) is technically true.\n\nBut the finding's SPECIFIC, distinctive claim is not demonstrable. Its exploit scenario requires \"two completion callbacks for two DISTINCT games\" finishing in one render tick, under-incrementing so a student who finished 3 games is recorded as 2, costing credit. That path does not exist:\n- `activeGameId` is a single state value (line 141). Only one game is the active/viewed game at a time. All three completion paths key off `activeGameId`/`gameData` (lines 243, 308, 490). Two distinct games cannot resolve through handleGameEnd in the same tick. So the cross-game under-increment scenario is unreachable.\n- A dedup guard exists: `lastCountedGameRef` (line 301), checked at lines 306 and 488, ensures \"only count once per game ID.\"\n\nThe genuinely reachable defect is a different one: the AI winning-move path (line 243) calls handleGameEnd WITHOUT setting lastCountedGameRef, so the subscription effect (line 308) then fires a second time for the same game — an OVER-count, not the under-count this finding describes, and explicitly attributed by the finding itself to \"finding #1.\" \n\nSo under the code-accuracy lens: the line of code is real, but (a) the stated exploit scenario maps to no reachable code path, (b) a mitigating control (lastCountedGameRef dedup + single activeGameId) defeats the described failure, and (c) the only real double-fire is the opposite-direction case belonging to another finding. The distinctive claim of THIS finding is not concretely demonstrable. Refuted.",
            "sev": "low"
          }
        ]
      },
      {
        "title": "ConnectFour: handleAnswerQuestion sets selectedIdx to null then renders option highlight comparisons that misbehave",
        "dimension": "general-correctness-bugs",
        "severity": "low",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "Verified all cited lines in /Users/lukemccarthy/pantherlearn/src/components/blocks/ConnectFourBlock.jsx. The code facts are accurate: line 153 defaults selectedIdx to -1, line 459 resets to -1 on a new question, line 469 sets it to chosenIdx, line 477 (inside setTimeout) resets to null. So there ARE two different sentinels (-1 and null). However, the claimed BEHAVIORAL bug does not occur: (1) selectedIdx is read in only two places, lines 980 and 995, and BOTH are gated behind `questionLocked === true` (line 975 wraps 980; line 995 begins with `questionLocked && ...`). (2) When line 477 sets null, it runs in the same setTimeout batch as setQuestionLocked(false) (476) and setShowQuestion(false) (475) — so the overlay is closed and locked is false; neither read path is active. (3) The finding's specific claim that \"a subsequent question briefly inherits the wrong type until line 459 resets it\" is false: grep confirms setShowQuestion(true) appears ONLY at line 462, and line 459 (setSelectedIdx(-1)) immediately precedes it in the same synchronous function body. React batches these updates, so the overlay never renders with selectedIdx===null while visible — by the time showQuestion flips true, selectedIdx is already -1 and questionLocked is false. There is no render window where null is observed by a comparison that matters. The finding itself concedes \"harmless,\" \"latent inconsistency rather than a crash,\" \"Cosmetic correctness only,\" \"No data loss.\" This is a code-tidiness nit (consistent sentinel preferred), not a demonstrable correctness bug. Severity downgraded to info.",
            "sev": "info"
          }
        ]
      },
      {
        "title": "EmbeddingExplorer: handleExplanationChange produces sparse arrays that serialize to Firestore with null holes and break scoring/ordering",
        "dimension": "general-correctness-bugs",
        "severity": "medium",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "The cited code exists exactly as quoted (EmbeddingExplorer.jsx lines 239-252, 268-270, 278-279), and the sparse-array mechanism is real: handleExplanationChange does `next[index]=text` where index is the dense map index of identifiedFindings in PhaseReport (line 1529: `identifiedFindings.map((findingId, idx) => ... onExplanationChange(idx,...))`), so typing into a higher box first yields holes that updateDoc serializes to [null,null,'text']. That part is accurate.\n\nBut the finding's load-bearing harm claims are FALSE:\n\n1) calculateScore (embeddingStore.js lines 156-162) does NOT index-pair explanations to findings. It only COUNTS non-empty entries: `explanations.filter((e) => e && e.length > 30).length`, then divides by totalFindings. The truthy guard `e && e.length>30` harmlessly skips null holes. The 'C text' sitting at index 2 is non-null and >30 chars, so it IS counted — the student does NOT lose the point. The exploit scenario ('calculateScore pairs explanation index 0 (null) with finding A and never sees the C text') is fabricated; no positional finding↔explanation join exists anywhere in scoring or grading.\n\n2) Not splicing on finding removal causes at most cosmetic mislabeling in the textarea UI (a typed explanation appearing under a different finding's box), never mis-scoring or grade loss, because scoring is a count not a positional pairing.\n\n3) PhaseReview (lines 1587-1668) renders only score breakdown + missed insights — it never displays explanations. The teacher grading component EmbeddingExplorerReview.jsx reads a different root collection ('embedding_explorer') and never touches engineerReport.explanations. The 'renders incorrectly' claim is unsupported.\n\nSo the only true effect is benign null padding in a Firestore array that the sole consumer already tolerates. No data corruption, no mis-scoring, no grade integrity impact. Category data-corruption and severity medium are unwarranted; this is at most an info-level cosmetic nit.",
            "sev": "info"
          }
        ]
      },
      {
        "title": "StudentProgress: getStudentLessonGrade can divide by zero / produce NaN grade when all graded items have max 0",
        "dimension": "general-correctness-bugs",
        "severity": "low",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "Verified against /Users/lukemccarthy/pantherlearn/src/pages/StudentProgress.jsx lines 405-499. The cited code exists exactly as quoted, but the finding's headline claim is self-refuted and the residual claim is not demonstrable.\n\n1) NaN / divide-by-zero (the title): IMPOSSIBLE. Line 494 returns null when `gradedItems.length === 0`. Line 497 computes `possible = gradedItems.reduce((s,i)=>s+(i.max||1),0)`, so every graded item contributes at least 1 to the denominator. Therefore whenever the division at line 499 executes, `possible >= gradedItems.length >= 1 > 0`. There is no code path to `possible === 0`. The finding's own description concedes this (\"`possible` can be 0 only if... the `||1` fallback is bypassed (it isn't for the reduce)\"). The title is therefore false.\n\n2) \"missing `||1` on the `earned` numerator\": this is correct behavior, not a bug. `earned` is the sum of points a student actually earned; applying a `||1` floor to it would invent free credit. The `||1` belongs only on the denominator as a zero-guard.\n\n3) Residual claim — \"weight:0 embed yields misleading 0%\": requires a teacher/seed to author `weight: 0` on a scored embed. Grepped all ~472 seed scripts: the minimum weight authored anywhere is `weight: 1` (counts: 32×weight:1, 61×weight:5, etc.); `weight: 0` appears ZERO times. Project convention (feedback_embed_weight_5) is weight:5 for scored embeds. So the trigger does not exist in the codebase; the finding labels it \"misconfigured\" itself. Even in that contrived case, the embed contributes max 0→(0||1)=1 to the denominator (minor dilution), and a 0% lesson grade only results if it is the SOLE graded item — a degenerate config where the teacher set the only graded activity to weight 0, which is intentionally weightless by definition.\n\nNet: code matches the quoted lines, but the stated impact (NaN/divide-by-zero, line-499 crash, grade corruption) does not hold; the only residual is a hypothetical degenerate config that exists nowhere in the data. Not a real, demonstrable bug.",
            "sev": "info"
          }
        ]
      },
      {
        "title": "StudentMana: controlled <select value={courseId}> can be null/undefined causing an uncontrolled-input warning and selection desync",
        "dimension": "general-correctness-bugs",
        "severity": "low",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "The cited code exists exactly as quoted (StudentMana.jsx: selectedCourse init at 333, courseId=selectedCourse at 334, the courses.length>1 select with value={courseId} at 758-768, and the load effect with setCourses + setSelectedCourse at 419-432). However, the finding's causal premise is wrong, so the bug does not manifest.\n\nThe finding assumes a render window where courses.length>1 is true but courseId is still null, attributed to 'setCourses ... before the setSelectedCourse effect commits.' But setCourses(list) and `if (!selectedCourse && list.length>0) setSelectedCourse(list[0].id)` are NOT two separate effects — they are two synchronous calls executed back-to-back inside the same async callback continuation (lines 428-429). The project runs React 19 (package.json: react ^19.2.0), where automatic batching applies to updates within the same execution context, including async/await continuations. Both setState calls batch into a single commit, so there is no intermediate render where courses is multi-item but selectedCourse is null.\n\nMount timing also rules out the warning: on initial mount courses=[] so courses.length>1 is false and the <select> is not rendered at all. The <select> only mounts in the batched commit that simultaneously sets courses (length>1) and selectedCourse — its very first render already has a defined string value. The React 'uncontrolled->controlled' warning requires a mounted element's value to transition from null/undefined to a string across renders; that cannot occur here. The routeCourseId path likewise keeps selectedCourse non-null from the start.\n\nThe proposed value={null} render and the desync window are not demonstrable. This is a non-issue under the actual code and React version.",
            "sev": "info"
          }
        ]
      },
      {
        "title": "Students can write question analytics and inject custom questions",
        "dimension": "pantherprep-audit",
        "severity": "medium",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "The raw rule claim is accurate: /Users/lukemccarthy/pantherprep/firestore.rules lines 26-34 do grant questionStats `write: if isPapsUser()` and customQuestions `create: if isPapsUser()`, and isPapsUser() (lines 8-13) is satisfied by any authenticated @paps.net student. So a student CAN write both collections. However, the finding is REFUTED because both stated impacts are materially false against the actual code:\n\n1. \"Corrupts the adaptive engine's difficulty/selection logic that feeds every student's practice\" — FALSE. The adaptive engine (lib/adaptive/, lib/practice-question-source.ts) has ZERO references to questionStats. It selects from questionPool (which IS locked: `write: if isPapsUser() && isStaff(...)`, rules l.83) weighted by owner-scoped performanceLog/adaptiveProfile. questionStats is read only by loadQuestionStats() (public/index.html:1426) and consumed only inside the teacher Dashboard's QuestionsTab (l.1638) to show \"Student Analytics\" in a preview modal. Poisoning it corrupts a teacher-facing analytics readout, not any student's selection.\n\n2. \"customQuestions surface in classmates' sessions / stored-XSS\" — FALSE. customQs is loaded only into the teacher Dashboard (l.1871) and concatenated into the static bank (allQ=QS.concat(_cq), l.1649) ONLY inside QuestionsTab for the teacher to browse/preview. It is never pulled into a student PracticeRunner/session. Rendering goes through renderPreview → sanitizeHtml (l.479), which strips non-allowlisted tags, so it isn't a live XSS sink even in the teacher view.\n\nReal exposure: a student can pollute a teacher-only analytics collection and inject junk into a teacher-only question browser — a data-integrity nuisance an admin sees, not a grade/adaptive-engine integrity hole or cross-student XSS. The finding's exploit_scenario does not hold against the code, and severity (claimed medium) is overstated; actual is low. Treating the materially-inaccurate finding as refuted per instructions. The over-permissive rule is genuine and worth hardening to isStaff, but the claimed harm is not real.",
            "sev": "low"
          }
        ]
      },
      {
        "title": "No client-side API key leak; translateText Cloud Function is properly auth-gated (informational pass)",
        "dimension": "pantherprep-audit",
        "severity": "info",
        "confirmCount": 0,
        "verifierCount": 1,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "This finding is an informational \"verified-good\" pass, not a vulnerability — so there is no exploitable issue to confirm. real=false because the finding asserts no security problem exists, and I verified that assertion is correct (i.e., there is nothing actionable/real to flag as a defect).\n\nLine-by-line verification of every claim:\n1. /Users/lukemccarthy/pantherprep/lib/firebase-config.ts l.2: apiKey \"AIzaSyD_okYkxb0Faye3kV2GLlVjyMI6CCs8apg\" is the Firebase web apiKey (alongside authDomain/projectId/storageBucket/appId for pantherprep-a5a73) — confirmed safe-to-ship, not a provider secret.\n2. Grep of lib/app/components/contexts/hooks for sk-/sk-ant-/other AIza keys (excluding the known web key) returned ZERO matches — no Gemini/OpenAI/Anthropic secrets in client code. Confirmed.\n3. functions/index.js translateText (l.19-47): verifies authorization header starts with 'Bearer ' (401 if missing), calls getAuth().verifyIdToken(token), rejects if !decoded.email.endsWith('@paps.net') with 403, 403 on invalid token — exactly as claimed.\n4. CORS pinned to ['https://pantherprep.web.app','https://pantherprep.firebaseapp.com']; POST-only (405 otherwise); payload caps enforced (MAX_TEXT_LENGTH 5000, MAX_TOTAL_LENGTH 500000, max 128 texts, 50-per-batch Firestore reads) — confirmed l.48-140.\n5. Google Cloud Translate credential is server-side: TranslationServiceClient() uses ADC, no key in client; project from process.env.GCLOUD_PROJECT. Confirmed.\n6. No spoofable score path: grep for postMessage/activityScore in lib/app/components/contexts/hooks returned nothing — pantherprep has no EmbedBlock-style score embeds. Confirmed.\n7. Dir layout claim accurate: app/components/lib/contexts/hooks at repo root, out/ present, pantherprep-adaptive is a separate scaffold. Confirmed.\n8. The @paps.net-only domain note (excludes @lachlan.internal) is accurate and correctly flagged as harmless/behavioral.\n\nThe finding is factually accurate in every particular and correctly classified as info. There is no defect to confirm and nothing to refute as a security hole — the code does exactly what the finding says, and what it says is \"this is clean.\"",
            "sev": "info"
          }
        ]
      },
      {
        "title": "57 untracked physics/AI embed tools in tools-public/ are the live embeds for physics-2026 lessons and have no committed copy",
        "dimension": "uncommitted-work-forensics",
        "severity": "high",
        "confirmCount": 1,
        "verifierCount": 3,
        "verdicts": [
          {
            "real": false,
            "confidence": "high",
            "reasoning": "Every load-bearing claim is contradicted by ground truth. (1) The finding claims \"25 of the U2-6 seed scripts contain paps-tools references\" — FALSE: zero seed scripts reference paps-tools; the named files (seed-phys-u[2-6]-*.cjs) do not exist; across the entire working tree \"paps-tools\" appears only in firebase.json (the hosting target) and src/components/blocks/EmbedBlock.jsx (an origin-allowlist regex). No lesson or seed embeds any of these 57 tools. (2) The claim that these are \"the live embeds for physics-2026 lessons\" is false — the OpenSciEd physics-2026 lessons are not seeded (project note + commit 25f39cf: \"lessons (Plan 3) + deploy remain\"); no live graded activity uses them. (3) The claim \"exist only as untracked files, not in any commit\" is false — the full canonical source for ALL of these tools (collision-sim-1d, crumple-zone-designer, mantle-convection, plate-boundary-builder, seismic-tomography, star-lifecycle, orbit-gravity-sim, em-spectrum, hr-diagram, vector-addition, stopping-distance, force-and-motion-basics, etc.) is committed on branch openscied-u1-grid-sim under public/tools/, WITH unit tests (.test.mjs). I diffed two untracked tools-public/ copies against the committed branch source: byte-identical (collision-sim 17887==17887; mantle-convection IDENTICAL). The untracked tools-public/ files are deploy-staging duplicates of committed source, fully recoverable via git checkout openscied-u1-grid-sim -- public/tools/. The 3 actually-deployed U1 embeds (grid-reliability, induction, energy-flow) are committed in tools-public/ on main (commit 25f39cf) AND have canonical source on the branch. Exploitability: no end-to-end path — no student-facing lessons reference these tools, and source is not at risk of irrecoverable loss. At most a minor hygiene note (staging dupes untracked on main), not high-severity work-at-risk.",
            "sev": "info"
          },
          {
            "real": true,
            "confidence": "medium",
            "reasoning": "Core fact is real but the evidence chain is substantially fabricated and severity is overstated. CONFIRMED: 57 untracked files in /Users/lukemccarthy/pantherlearn/tools-public/ (git status --short matches); firebase.json:94-95 confirms tools-public is the deploy dir for target paps-tools → paps-tools.web.app; files are real hand-authored HTML source (13-18KB each), not generated, not gitignored; they exist on no branch/commit so a `git clean -fdx` would lose them. That underlying work-at-risk concern is legitimate.\n\nBut the finding's specific evidence is FALSE in several load-bearing ways:\n1. \"25 of the U2-6 seed scripts contain paps-tools references\" — FALSE. Zero .cjs seeds in the repo reference paps-tools. The glob scripts/seed-phys-u[2-6]-*.cjs matches nothing; OpenSciEd physics seeds aren't even in this repo's scripts/. Only firebase.json, .firebaserc, and src/components/blocks/EmbedBlock.jsx reference paps-tools.\n2. \"tools-public is a newer parallel deploy dir that was never added to git\" — FALSE. tools-public/ already has 20 TRACKED files committed (git ls-files tools-public/ = 20). The 57 are net-new additions on top of an already-tracked dir, not an untracked dir.\n3. \"the committed embed set lives in public/tools (24 files)\" as the counterpart — FALSE; the physics tools are not committed in public/tools either (only coulombs-law-data-lab.html overlaps, a different tool). There is no committed copy anywhere — but framing it as \"parallel to public/tools\" misrepresents the layout.\n4. \"physics-2026 live lessons embed these tools / live graded activities\" — overstated. These are 2026-27 OpenSciEd staging work (files dated Jun 19, branch main, no Firestore seeds reference them). Not live, not currently graded.\n5. \"Several of these tools are scored embeds\" — of the physics/space tools the finding lists, only coulombs-law-data-lab posts activityScore (1 of ~18). The \"scored\" tools are mostly the AI-literacy set, not the emphasized physics simulations.\n\nSo: genuine uncommitted-source risk (real=true), but it's staged future-year work in an already-partially-tracked dir, the 25-seed evidence is fabricated, and the live-grade-loss exploit scenario does not currently apply. Severity adjusted high → low. Mitigation note: EmbedBlock.jsx:18 has a tightened origin allowlist for paps-tools embeds, so the scored-embed spoofing angle is separately guarded.",
            "sev": "low"
          },
          {
            "real": false,
            "confidence": "high",
            "reasoning": "REFUTED. The finding's central claim — that the 57 untracked tools-public/ files (specifically the OpenSciEd physics embeds) \"have no committed copy ... not in any commit, not on origin\" — is false. Verified facts:\n\n1. All 18 of the named at-risk physics tools (collision-sim-1d.html, crumple-zone-designer.html, mantle-convection.html, seismic-tomography.html, star-lifecycle-model.html, etc.) ARE committed in public/tools/ on branch openscied-u1-grid-sim. The untracked working-tree copies are byte-for-byte identical to the committed branch copies (verified via shasum on 5 samples: all MATCH).\n\n2. That branch IS pushed to origin: git ls-remote shows refs/heads/openscied-u1-grid-sim = 5cdd656, committed today (2026-06-20). So the source is committed AND backed up remotely. The mitigating control the lens asks for exists: a pushed branch holds identical copies.\n\n3. The finding's evidence \"grep -rl paps-tools seed-phys-u[2-6]-*.cjs = 25 seed scripts\" is false on main — those seed files do not exist on main (glob: no matches), and NO file anywhere on main references paps-tools.web.app or these tool names. The seed-phys-u*.cjs scripts AND the committed tool copies all live together on the openscied-u1-grid-sim branch. Nothing is live/embedded in current main lessons.\n\n4. Per project memory (project_openscied_physics.md), OpenSciEd physics is 2026-27 work-in-progress; lessons \"remain\" unbuilt and nothing is deployed/live. So the \"students will use next year\" / \"scored embeds for live graded activities\" framing overstates exposure — there are no live grades attached.\n\n5. Exploit scenario (git clean -fdx irrecoverably wipes source) is defused: git checkout openscied-u1-grid-sim -- public/tools/ restores everything; the branch is on origin.\n\nThe only true sub-fact is that these files are untracked in main's working tree — but only because the wrong branch is checked out. The work is fully committed on its own pushed branch. No work-at-risk, no grade-security hole. Severity drops to info (housekeeping: the working tree on main has another branch's build output sitting in it).",
            "sev": "info"
          }
        ]
      }
    ]
  }
}